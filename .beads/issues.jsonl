{"_type":"issue","id":"remote_comfy_generator-cwt","title":"_find_file_by_sha: hashing entire dest_dir takes minutes on populated volumes","description":"When the CivitAI API lookup succeeds and returns expected_sha + filename, _find_file_by_sha tries the hint_name first, then falls through to scanning AND hashing every other file in dest_dir. On a populated checkpoints/ dir with multiple multi-GB models, this takes 2-3 minutes per civitai download. Live log shows 2:31 gap between dispatch and first subprocess output. Fix: when hint_name is supplied, only check that one file. Skip the exhaustive fallback scan — the rare 'same bytes, different filename' edge case isn't worth the per-download minutes.","status":"closed","priority":0,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T05:01:51Z","created_by":"Hearmeman24","updated_at":"2026-05-27T05:04:20Z","started_at":"2026-05-27T05:01:56Z","closed_at":"2026-05-27T05:04:20Z","close_reason":"hint-only dedup; removed exhaustive directory hash; 3 new/updated tests including a perf-guard that fails loudly if hashing leaks back in; shipped in remote-comfy-gen-handler@09f3b69","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-bmq.1","title":"P0 A.7.5: env-first token lookup, deprecated argv flag fallback","description":"comfy_gen/cli.py install-preset: civitai_token = os.environ.get('COMFY_GEN_CIVITAI_TOKEN') or args.civitai_token (same for HF). Mark --civitai-token / --hf-token deprecated in --help; emit stderr warning if used. Keep flag fallback for one release so old BlockFlow builds don't break. After this ships + tags, BlockFlow drops argv passing (sgs-ui-h1c.1.4).","status":"closed","priority":0,"issue_type":"task","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T16:24:46Z","created_by":"Hearmeman24","updated_at":"2026-05-26T16:58:27Z","started_at":"2026-05-26T16:55:51Z","closed_at":"2026-05-26T16:58:27Z","close_reason":"env-first token lookup landed in cli.py; argv flags marked deprecated with stderr warning; 7 new tests cover all paths","labels":["audit","blockflow","contract","cross-repo","p0","security"],"dependencies":[{"issue_id":"remote_comfy_generator-bmq.1","depends_on_id":"remote_comfy_generator-bmq","type":"parent-child","created_at":"2026-05-26T19:24:46Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-9oi","title":"worker.on_progress: dedupe identical log lines to stop ImageUpscale log flood","description":"ComfyUI's WebSocket emits a progress tick every ~100ms during certain nodes (notably ImageUpscaleWithModel). Worker's on_progress emits one log line per tick. With identical message text, the log gets ~150 redundant lines per node, making it hard to read and creating false 'stall' impressions. Fix: dedupe by message text — if the formatted log line is identical to the previous, suppress the log (RunPod IN_PROGRESS still fires). Per-sampler step logs (KSampler Step N/M) are unique each tick, so they're unaffected.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-28T01:03:03Z","created_by":"Hearmeman24","updated_at":"2026-05-28T01:04:52Z","started_at":"2026-05-28T01:03:08Z","closed_at":"2026-05-28T01:04:52Z","close_reason":"shipped in remote-comfy-gen-handler@3d209bd; 4 new tests","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-6zu","title":"download_handler: use model hash cache for dedup without rehash","description":"Use /runpod-volume/.model-hash-cache.json before hashing existing model files in download_handler. If path size+mtime match and cached sha equals expected sha, skip download without reading the multi-GB file. After aria2c --checksum succeeds, persist expected sha plus size+mtime to the same cache without a post-download hash pass. Preserve fallback to one-file hashing when cache is missing or stale.","status":"closed","priority":1,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T06:30:37Z","created_by":"Hearmeman24","updated_at":"2026-05-27T06:36:04Z","started_at":"2026-05-27T06:30:39Z","closed_at":"2026-05-27T06:36:04Z","close_reason":"Implemented downloader hash cache fast path and /workspace CPU installer cache-root mapping; verified with pytest.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-r53","title":"CivitAI metadata lookup hides real failure and does not retry","description":"Download jobs can fail with 'CivitAI API metadata lookup failed... Verify CIVITAI_TOKEN' even when CIVITAI_TOKEN is set. _civitai_version_metadata collapses HTTP/URL/JSON failures to None, and _download_civitai emits a token-oriented generic error. Add bounded retries for transient metadata failures and preserve HTTP status/body or URL error details so token/config issues are distinguishable from CivitAI transient failures.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T06:24:41Z","created_by":"Hearmeman24","updated_at":"2026-05-27T06:27:58Z","started_at":"2026-05-27T06:25:03Z","closed_at":"2026-05-27T06:27:58Z","close_reason":"CivitAI metadata lookup now retries transient HTTP/URL failures, preserves HTTP status/body in errors, and falls back to caller-provided filename+sha for preset entries such as MoP when metadata is unavailable. Verified token works locally for MoP CivitAI versions 2960578 and 2932204; 189 tests pass.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-zws","title":"download command can remain IN_PROGRESS after worker finishes","description":"Download jobs can log 'Finished' / 'Download complete' while RunPod status still reports IN_PROGRESS. Workflow jobs in worker.py already sleep before returning because RunPod SDK progress_update can race result completion and overwrite status back to IN_PROGRESS (runpod-python issue #250). The download command dispatch returns directly from download_handler.handle(job), bypassing that grace period. Investigate and fix result-ack/progress race for non-workflow command handlers, especially download.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T06:18:10Z","created_by":"Hearmeman24","updated_at":"2026-05-27T06:21:59Z","started_at":"2026-05-27T06:20:12Z","closed_at":"2026-05-27T06:21:59Z","close_reason":"Download command dispatch now uses the same one-second progress/result grace as workflow jobs before returning or re-raising, preventing final progress_update from racing the RunPod result completion. Added worker dispatch tests for success and exception paths.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-bvf","title":"download_handler: make RunPod progress updates reflect parallel completion","description":"RunPod status can show stale progress like Cached 2/4 at 50% even after logs show Download complete. The parallel downloader emits per-index progress, and URL cached hits do not emit cached progress, so the latest stored progress can lag actual completion. Track completed file count under the progress lock and emit monotonic per-file completion updates plus a final Done update before returning.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T06:14:51Z","created_by":"Hearmeman24","updated_at":"2026-05-27T06:16:41Z","started_at":"2026-05-27T06:14:52Z","closed_at":"2026-05-27T06:16:41Z","close_reason":"Progress now reports completed file count rather than input index, and every file completion emits a RunPod progress update. Added regression coverage for all-cached parallel batches showing Cached 1/4 through Cached 4/4.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-kts","title":"download_handler: enforce aria2c timeout while streaming stdout","description":"The improved downloader can stall after CivitAI metadata resolution because _download_url reads aria2c stdout with a blocking for-line loop and only calls proc.wait(timeout=...) after stdout reaches EOF. If aria2c hangs without emitting output, the handler never reaches the timeout. Add a regression test and make the streaming loop enforce timeout while the process is still running.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T05:47:23Z","created_by":"Hearmeman24","updated_at":"2026-05-27T05:49:51Z","started_at":"2026-05-27T05:47:36Z","closed_at":"2026-05-27T05:49:51Z","close_reason":"Fixed _download_url so aria2c stdout streaming is timeout-enforced; silent hung aria2c is killed, partial files are removed, and startup logging identifies the active file. Added regression tests for silent process timeout and _download_url bounded failure.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-cdb","title":"download_handler: CivitAI direct aria2c (replace wrapped script + post-hash)","description":"Replaced the wrapped Hearmeman24/CivitAI_Downloader subprocess with a thin wrapper that resolves filename + sha256 + downloadUrl via the CivitAI API, then hands off to _download_url with expected_sha and a Bearer-token --header (when CIVITAI_TOKEN is set). aria2c verifies in-flight via --checksum, so the post-download re-hash that was taking ~75-100 MB/s × file-size on the network volume (3-4 min per 17GB file) is gone. Same correctness guarantees; ~10 min saved per typical multi-file preset install. Shipped in remote-comfy-gen-handler@9354fdd. 7 new tests cover metadata-lookup failure, dedup hit, cache miss handoff, auth-header plumbing, caller-sha override.","status":"closed","priority":1,"issue_type":"feature","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T05:32:20Z","created_by":"Hearmeman24","updated_at":"2026-05-27T05:32:31Z","closed_at":"2026-05-27T05:32:31Z","close_reason":"shipped in remote-comfy-gen-handler@9354fdd; 7 new tests; 181 total passing","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-8hh","title":"download_handler: parallel download manager (2 dl × 1 verify)","description":"Replaced sequential loop in handle() with ThreadPoolExecutor(max_workers=2). Each task owns one spec end-to-end. URL/HF: aria2c --checksum in-flight verify, no post-hash. CivitAI: post-hash submitted async to a single-thread verify pool, next file dispatches immediately. Results ordered by input index. Progress lock-serialized. First-error captured, downloads complete, job fails atomically. Shipped in remote-comfy-gen-handler@746a21e; 3 new tests (overlap, ordering, partial-failure).","status":"closed","priority":1,"issue_type":"feature","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T05:23:09Z","created_by":"Hearmeman24","updated_at":"2026-05-27T05:23:20Z","closed_at":"2026-05-27T05:23:20Z","close_reason":"shipped in remote-comfy-gen-handler@746a21e; 185 tests passing including 3 new parallelism guards","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-8r7","title":"download_handler: log entry + verify-progress so blocking steps aren't silent","description":"Two silent gaps in the civitai path: (1) between 'CivitAI download: version X -\u003e dest' dispatch and the script's first stdout — that's the API lookup + dedup hash + subprocess spawn, ~0.5-5s; (2) between the script's 'Model ready at:' line and our 'Downloaded:' log — that's the post-download sha256 verify (~30-180s on multi-GB files over network volume). Add: [civitai] entering download for version X log at the top of _download_civitai; [job …] verifying sha256 of \u003cname\u003e (\u003csize_mb\u003e MB)… before the post-download hash; [job …] sha256 verified in \u003cs\u003es after; periodic heartbeat ([job …] still hashing \u003cname\u003e at \u003celapsed\u003es) every 15s during the hash. No semantic change — pure observability fix.","status":"closed","priority":1,"issue_type":"task","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T05:10:28Z","created_by":"Hearmeman24","updated_at":"2026-05-27T05:12:17Z","started_at":"2026-05-27T05:10:33Z","closed_at":"2026-05-27T05:12:17Z","close_reason":"entry log + verify heartbeats shipped in remote-comfy-gen-handler@4e330dc; covers both silent gaps (dispatch-\u003esubprocess and Model-ready-\u003eDownloaded); 1 new test for the heartbeat","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-xud","title":"worker: allowlist models that auto-download at runtime (rife49.pth, etc.)","description":"_check_models_exist preflight fails the job when models like rife49.pth are missing, even though the RIFE VFI custom node downloads them on first use. Add an allowlist of runtime-auto-downloaded filenames: hardcoded defaults (rife49.pth and known siblings) + COMFY_GEN_IGNORE_MISSING_MODELS env var (comma-separated) for users to extend without code changes. Filter inside _check_models_exist so allowlisted files don't appear in the missing list. Surface them under a separate auto_downloaded_models key in the response for visibility.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T18:27:26Z","created_by":"Hearmeman24","updated_at":"2026-05-26T18:29:29Z","started_at":"2026-05-26T18:27:33Z","closed_at":"2026-05-26T18:29:29Z","close_reason":"Built-in rife40-49.pth allowlist + COMFY_GEN_IGNORE_MISSING_MODELS env var override; _check_models_exist returns (missing, auto_downloaded); shipped in remote-comfy-gen-handler@9e5fb15; 7 tests","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-6i0","title":"worker: log runtime commit SHA at boot + every handler dispatch","description":"No way to tell from RunPod logs which commit of serverless-runtime a worker is actually running. FlashBoot can restore a Python process snapshot at an old SHA even when the on-disk clone is current. Print '[start] runtime commit \u003csha\u003e: \u003csubject\u003e' in start.sh + '[handler] runtime commit \u003csha\u003e' at the top of handler() to make the running version inspectable from any single job's log.","status":"closed","priority":1,"issue_type":"task","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T18:22:27Z","created_by":"Hearmeman24","updated_at":"2026-05-26T18:24:11Z","started_at":"2026-05-26T18:22:32Z","closed_at":"2026-05-26T18:24:11Z","close_reason":"start.sh prints [start] runtime commit \u003csha\u003e: \u003csubj\u003e + exports RUNTIME_COMMIT; worker.py prints at module-load and at every handler() call. Pair start.sh + per-job stamps to detect FlashBoot snapshot-vs-disk drift. Shipped in remote-comfy-gen-handler@9b10d05","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-15u","title":"_download_civitai: parse 'Model ready at:' instead of dir-diff (resume case)","description":"When the destination already has stale files from a prior attempt, after-before listdir diff is empty even though the CivitAI script wrote the file. Fails fully-successful 14GB+ downloads. Parse the script's own '✅ Model ready at: \u003cpath\u003e' line as the authoritative answer; fall back to listdir diff if the marker isn't found.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T17:26:30Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:28:44Z","started_at":"2026-05-26T17:26:39Z","closed_at":"2026-05-26T17:28:44Z","close_reason":"parse 'Model ready at:' as primary path, .aria2-filtered listdir diff as fallback; 2 regression tests; worker change shipped in remote-comfy-gen-handler@cdcc522","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-dkn","title":"installer image: bundle CivitAI_Downloader (CPU preset installs fail without it)","description":"CPU pod preset install fails when preset contains a civitai entry: RuntimeError: CivitAI download failed (exit 2): python3: can't open file '/tools/civitai-downloader/download_with_aria.py': [Errno 2] No such file or directory.\n\nRoot cause: download_handler.py:32 hardcodes /tools/civitai-downloader/download_with_aria.py. The GPU base image (serverless-docker/Dockerfile lines 100-106) clones Hearmeman24/CivitAI_Downloader to that path; Dockerfile.installer never does, so installer-v5 is missing it.\n\nFix:\n1. Add the same git-clone block to serverless-docker/Dockerfile.installer (pin same SHA as Dockerfile).\n2. Tag installer-v6, push to trigger CircleCI image build.\n3. Bump DEFAULT_IMAGE in comfy_gen/install_preset.py:31 and --image default in comfy_gen/cli.py:731 to installer-v6.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T16:31:26Z","created_by":"Hearmeman24","updated_at":"2026-05-26T16:35:56Z","started_at":"2026-05-26T16:31:29Z","closed_at":"2026-05-26T16:35:56Z","close_reason":"installer-v6 published and comfy_gen defaults bumped","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-bmq.5","title":"P1 A.1.2/E.1: vendor progress regex into comfy_gen/_progress_format.py + contract test","description":"Create comfy_gen/_progress_format.py with module-level docstring spec for the stderr progress line format. Canonical regex: r'\\[(\\d+)s\\]\\s+(\\w+):\\s+\\((\\d+)/(\\d+)\\)\\s*(.*)'. Contract test emits each progress event type and asserts the regex matches every line. Prevents silent breakage of BlockFlow's progress bar when stderr line format drifts.","status":"closed","priority":1,"issue_type":"task","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T16:24:52Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:10:49Z","closed_at":"2026-05-26T17:10:49Z","close_reason":"canonical PROGRESS_RE in comfy_gen/_progress_format.py; download._progress + poller default branch reformat N/M messages; 10 contract tests","labels":["audit","blockflow","contract","contract-test","p1"],"dependencies":[{"issue_id":"remote_comfy_generator-bmq.5","depends_on_id":"remote_comfy_generator-bmq","type":"parent-child","created_at":"2026-05-26T19:24:52Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-bmq.4","title":"P1 A.6.3: comfy-gen download accepts --timeout SECONDS","description":"Today the download verb has no timeout flag; BlockFlow hardcodes 30 min and large-preset downloads exceed that. Accept --timeout SECONDS and plumb to polling loop. BlockFlow computes from preset disk_size_estimate_gb (sgs-ui-h1c.1.8).","status":"closed","priority":1,"issue_type":"task","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T16:24:51Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:08:57Z","closed_at":"2026-05-26T17:08:57Z","close_reason":"--timeout already on CLI; now plumbed end-to-end to worker subprocess timeouts so BlockFlow's 300+size_gb*60 actually scales aria2c/civitai","labels":["audit","blockflow","contract","cross-repo","p1"],"dependencies":[{"issue_id":"remote_comfy_generator-bmq.4","depends_on_id":"remote_comfy_generator-bmq","type":"parent-child","created_at":"2026-05-26T19:24:50Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-bmq.3","title":"P1 A.7.2: add error_code enum to install_error SSE events","description":"install-preset SSE event install_error gets a new field: error_code: 'supply_constraint' | 'download_failed' | 'sha_mismatch' | 'preflight_disk_full' | .... Existing reason string stays for one release. BlockFlow matches on error_code (sgs-ui-h1c.1.7).","status":"closed","priority":1,"issue_type":"task","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T16:24:49Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:04:11Z","closed_at":"2026-05-26T17:04:11Z","close_reason":"error_code enum added to install_error + preflight_fail; codes defined in comfy_gen/_install_error_codes.py; worker-side string literal drift gate via parametrized test","labels":["audit","blockflow","contract","cross-repo","p1"],"dependencies":[{"issue_id":"remote_comfy_generator-bmq.3","depends_on_id":"remote_comfy_generator-bmq","type":"parent-child","created_at":"2026-05-26T19:24:49Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-bmq.2","title":"P1 A.7.6: new 'comfy-gen version --endpoint-id' verb","description":"Returns {ok: true, worker_version: '0.x.y'} for the given endpoint. Cheap call — no GPU needed (light worker handler path or boot-time constant). BlockFlow uses for semver gate against preset's comfygen_min_version before install-preset (sgs-ui-h1c.1.6).","status":"closed","priority":1,"issue_type":"task","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T16:24:48Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:01:45Z","closed_at":"2026-05-26T17:01:45Z","close_reason":"comfy-gen version verb implemented; dispatches health command and reshapes {ok, worker_version}; 5 tests","labels":["audit","blockflow","contract","cross-repo","p1"],"dependencies":[{"issue_id":"remote_comfy_generator-bmq.2","depends_on_id":"remote_comfy_generator-bmq","type":"parent-child","created_at":"2026-05-26T19:24:48Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-bmq","title":"BlockFlow contract audit: ComfyGen-side fixes (P0/P1/P2)","description":"# ComfyGen side of BlockFlow contract audit (P0/P1/P2)\n\nParent audit: `sgs-ui-h1c` in the sgs-ui beads workspace. This epic groups the ComfyGen-side tasks.\n\n## P0\n\n- **A.7.5 — token transport: argv → env var.** `install-preset` reads `COMFY_GEN_CIVITAI_TOKEN` and `COMFY_GEN_HF_TOKEN` from env first, then falls back to `--civitai-token` / `--hf-token` flags. Flags marked deprecated in `--help` and emit a stderr warning if used. Keep flag fallback for one release so old BlockFlow builds don't break. BlockFlow stops passing argv after this lands.\n\n  ```python\n  civitai_token = os.environ.get(\"COMFY_GEN_CIVITAI_TOKEN\") or args.civitai_token\n  hf_token     = os.environ.get(\"COMFY_GEN_HF_TOKEN\")     or args.hf_token\n  if args.civitai_token or args.hf_token:\n      print(\"warning: --civitai-token/--hf-token on argv is deprecated; use env vars\", file=sys.stderr)\n  ```\n\n## P1\n\n- **A.7.6 — new `comfy-gen version` verb.** Returns `{ok: true, worker_version: \"0.x.y\"}` for the given `--endpoint-id`. BlockFlow uses this for a semver gate against the preset's `comfygen_min_version` before running `install-preset`. Cheap call — no GPU needed.\n- **A.7.2 — `error_code` on `install_error` events.** Add an enum field to the SSE `install_error` event: `error_code: \"supply_constraint\" | \"download_failed\" | \"sha_mismatch\" | \"preflight_disk_full\" | ...`. Existing `reason` string stays. BlockFlow matches on `error_code`; keeps \"out of capacity\" string fallback for one release.\n- **A.6.3 — `comfy-gen download` accepts `--timeout`.** Today the verb has no timeout flag; BlockFlow hardcodes 30 min on its side and large-preset downloads can exceed that. Accept `--timeout SECONDS`, plumb to the polling loop. BlockFlow computes `300 + size_gb * 60` based on the preset's `disk_size_estimate_gb`.\n- **A.1.2 / E.1 — vendor the progress regex into the package.** Create `comfy_gen/_progress_format.py` with a module-level docstring spec and a contract test that emits each progress event type and asserts the canonical regex `r\"\\[(\\d+)s\\]\\s+(\\w+):\\s+\\((\\d+)/(\\d+)\\)\\s*(.*)\"` matches every line. Prevents silent breakage of BlockFlow's progress bar when stderr line format drifts. BlockFlow references the same spec via documentation; no runtime import (keeps repos decoupled).\n\n## P2\n\n- **B.3.1 / A.5.2 (ComfyGen half) — expose `volume_root` in `comfy-gen info`.** Add a `volume_root: \"/runpod-volume\"` (or whatever the worker actually mounts) field to the `info` JSON output. BlockFlow caches it and uses it to construct delete paths instead of the hardcoded `/runpod-volume/ComfyUI/models/loras/`.\n\n## Out of scope (filed elsewhere or deferred)\n\n- BlockFlow-side halves — `sgs-ui-h1c.1` in the sgs-ui beads workspace.\n- `blockflow-presets` settings-field lint — `sgs-ui-h1c.2`.\n- **E.2 unified stderr JSON-line protocol across all verbs** — deferred to a future epic; long-term refactor.\n\n## Coordination\n\nEach P1 item has a BlockFlow consumer waiting on a ComfyGen tag/release. Suggested rollout order:\n\n1. P0 (A.7.5) — ship + tag. BlockFlow then drops argv tokens and version-gates against this tag.\n2. P1 A.7.6 (version verb) — unblocks safer rollout of every later change.\n3. P1 A.7.2 / A.6.3 / progress regex — independent, can ship together.\n4. P2 volume_root — last.\n\n## Done when\n\n- All items above merged and a release tag is cut.\n- Contract tests for the progress regex run in ComfyGen CI.\n- BlockFlow's `sgs-ui-h1c.1` consumers verified green against the new release.\n","status":"closed","priority":1,"issue_type":"epic","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T16:23:14Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:12:30Z","closed_at":"2026-05-26T17:12:30Z","close_reason":"all 6 children shipped: bmq.1 env-first tokens, bmq.2 version verb, bmq.3 error_code, bmq.4 timeout plumbing, bmq.5 progress regex, bmq.6 volume_root. Worker-side changes in remote-comfy-gen-handler@4583bae/44e0f01/3ca58a5. 162 tests passing.","labels":["audit","blockflow","contract"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-0by","title":"smoke: retry-and-skip on RunPod CPU SUPPLY_CONSTRAINT + surface install-preset stdout errors","description":"## Symptom\n\nCI smoke (pipeline 46) failed all 4 preset jobs at ~15s with:\n`install-preset unexpected exit (1) with no terminal event. stderr tail:`\n(empty stderr).\n\n## Root cause (two stacked bugs)\n\n1. **Capacity issue:** every CPU SKU returned SUPPLY_CONSTRAINT. install-preset raises:\n   `pod spawn failed — no CPU instance available for any of [...]; last=...SUPPLY_CONSTRAINT...`\n\n2. **Harness swallows the error:** the RuntimeError is caught in `comfy_gen/cli.py:781-803`, routed through `output.error()` which writes `{\"status\":\"error\",\"error\":\"...\"}` to **stdout** and exits 1. `automation/smoke_preset.run_install_preset` (smoke_preset.py:151-187) parses stdout but only acts on `type ∈ {install_done, install_error, preflight_fail}` events — everything else is silently dropped. When the loop ends with `terminal is None` it dumps stderr (empty) but never echoes the stdout lines, so the real error vanishes.\n\n## Fix\n\n- smoke_preset.run_install_preset: buffer parsed stdout lines and include in the \"no terminal event\" RuntimeError so the underlying CLI error surfaces.\n- smoke_preset.main: detect SUPPLY_CONSTRAINT / 'no CPU instance available' in the error message; retry the install with exponential backoff (30s, 60s, 120s...), capped at 5 minutes total wall time. After 5 min give up and exit 0 with `{\"ok\": true, \"skipped\": true, \"reason\": \"CPU supply constraint\"}` so CI doesn't fail.\n- Tests for: (a) error surfacing when CLI emits status:error on stdout, (b) retry on supply-constraint, (c) skip-after-5min path.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T14:45:27Z","created_by":"Hearmeman24","updated_at":"2026-05-26T14:48:19Z","started_at":"2026-05-26T14:45:31Z","closed_at":"2026-05-26T14:48:19Z","close_reason":"Shipped in ae523fa: SupplyConstrainedError + run_with_retry (30/60/120s backoff, 5min default budget, skip on exhaustion) + surface CLI status:error stdout lines. 5 new tests, full suite 118 passing.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-uvm","title":"preset_resolver: honor source=civitai (extract version_id from URL)","description":"## Symptom\n\n`comfy-gen install-preset --preset-id \u003cpid\u003e` silently fails partway through (or sets up the wrong download) when the preset's models include `source: \"civitai\"` entries. The aria2c URL-source downloader gets handed the CivitAI URL without auth and returns 401/403.\n\n## Root cause\n\n`serverless-runtime/preset_resolver.py:40` `preset_to_download_batch` hard-codes `source: \"url\"` for every preset model regardless of what the preset declared:\n\n```python\ndef preset_to_download_batch(preset: dict) -\u003e list[dict]:\n    return [\n        {\n            'source': 'url',                    # \u003c-- always 'url'; ignores m['source']\n            'url': m['url'],\n            'destination_path': m['dest'],\n            'sha256': m['sha256'],\n        }\n        for m in preset.get('models', [])\n    ]\n```\n\nA preset declaring `source: 'civitai'` gets translated to a URL fetch. aria2c then hits `https://civitai.com/api/download/models/\u003cvid\u003e` with no auth header.\n\n## Worker side is ALREADY wired\n\n`serverless-runtime/download_handler.py` has a working CivitAI code path (`_download_civitai(version_id, dest_dir)`) that picks up `source: 'civitai'` payload entries and uses the authenticated downloader at `/tools/civitai-downloader/download_with_aria.py`. The auth token is forwarded via `job_input.get('civitai_token')` (line 304), which the install_preset CLI already sets via its `--civitai-token` flag.\n\nSo the GAP is purely the translator function — every other layer is ready.\n\n## Fix\n\nBranch on `m['source']` in `preset_to_download_batch`:\n\n```python\nimport re\n\nCIVITAI_VID_RE = re.compile(r'civitai\\.com/api/download/models/(\\d+)')\n\ndef preset_to_download_batch(preset: dict) -\u003e list[dict]:\n    out = []\n    for m in preset.get('models', []):\n        source = m.get('source', 'url')\n        dest = m['dest']  # 'unet/foo.gguf', 'loras/bar.safetensors', etc.\n        if source == 'civitai':\n            mo = CIVITAI_VID_RE.search(m['url'])\n            if not mo:\n                raise ValueError(\n                    f\"civitai source for {dest!r} requires URL matching \"\n                    f\"civitai.com/api/download/models/\u003cversion_id\u003e: got {m['url']!r}\"\n                )\n            version_id = mo.group(1)\n            subfolder, _, filename = dest.partition('/')\n            entry = {\n                'source': 'civitai',\n                'version_id': version_id,\n                'dest': subfolder,           # worker derives filename from response\n                'filename': filename,        # canonical name the workflow expects\n                'sha256': m['sha256'],\n            }\n        else:\n            entry = {\n                'source': 'url',\n                'url': m['url'],\n                'destination_path': dest,\n                'sha256': m['sha256'],\n            }\n        out.append(entry)\n    return out\n```\n\nNote: download_handler.py:268 documents the civitai payload shape as `{'source': 'civitai', 'version_id': '\u003cid\u003e', 'dest': '\u003csubfolder\u003e'}`. The optional `filename` field ensures the file lands with the name the workflow JSON references (CivitAI's filename-from-response isn't always predictable).\n\n## TDD\n\n`tests/test_preset_resolver.py` already exists — add cases:\n\n1. `test_civitai_source_translates_to_version_id`: input model {source:civitai, url:'.../models/2668710', dest:'unet/foo.gguf', sha256:...} → output {source:civitai, version_id:'2668710', dest:'unet', filename:'foo.gguf', sha256:...}\n2. `test_civitai_url_without_version_id_raises`: url='https://civitai.com/something-else' → ValueError\n3. `test_url_source_unchanged_for_huggingface`: existing happy path still produces `source: 'url'` + destination_path\n4. `test_mixed_sources_in_one_preset`: 3 HF models + 2 CivitAI → output has 3 url + 2 civitai entries, order preserved\n5. `test_civitai_with_trailing_query_params`: url='.../models/2668710?type=Model' → version_id='2668710' (regex tolerates trailing junk)\n\n## Live verification\n\nAfter fix lands and image rebuilds, install the BlockFlow wan22-svi-4pass preset (CivitAI version IDs 2668710 + 2668712). Verify:\n- `download_done` SSE events show the 2 GGUFs landing at `/runpod-volume/ComfyUI/models/unet/` with their canonical filenames\n- File sha256 matches the preset's declared sha256\n- BlockFlow's installed_paths persists both files\n\n## Discovered from\n\nBlockFlow bead sgs-ui-qv8 (in sgs-ui repo) — discovered while building the wan22-svi-4pass preset. That bead can be closed once this fix ships.\n\n## Files touched\n\n- `serverless-runtime/preset_resolver.py` (~25 lines)\n- `tests/test_preset_resolver.py` (~80 lines for 5 tests)\n\nNo docker rebuild needed beyond the next normal CI cycle since preset_resolver.py is cloned at boot per the install-preset CLI pattern.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T14:16:04Z","created_by":"Hearmeman24","updated_at":"2026-05-26T14:21:09Z","started_at":"2026-05-26T14:16:46Z","closed_at":"2026-05-26T14:21:09Z","close_reason":"Fixed in preset_resolver bc1a6ae (sibling) + tests fdef89a (main). source=civitai now routes to existing /tools/civitai-downloader/download_with_aria.py.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-d9v","title":"Installer pod /shutdown doesn't actually stop billing — orchestrator must DELETE","description":"# Installer pod /shutdown doesn't actually stop billing\n\nLive-observed in CircleCI smoke (and reproducible in the install-preset\nCLI flow): POST /shutdown returns 200, the Python process exits, but\nRunPod restarts the container and the install entrypoint runs again\n(\"[installer] starting\" in logs). Billing keeps running.\n\n## Logs (verbatim from a failing run)\n\n```\nDEBUG  | installer-hidream-o1 | Sending Progress Update: ...\n[job installe] Download complete: 1 file(s) in 122s\nweb_log.py : POST /install/hidream-o1 HTTP/1.1 200 4962 \"-\" \"comfy-gen/0.2\"\nERROR  | installer-hidream-o1 | Failed to return job results. | JOB_DONE_URL\u0026isStream=false\nweb_log.py : POST /shutdown HTTP/1.1 200 220 \"-\" \"comfy-gen/0.2\"\n[installer] starting                                            ← RESTART\nFrom https://github.com/Hearmeman24/remote-comfy-gen-handler\n * branch            main       -\u003e FETCH_HEAD\nHEAD is now at 9d88592 Merge installer-server (...)\n[installer] runtime at 9d88592c7a844725f783f4e35a51fba2be5536ad\n[installer] mode=server port=3000\n```\n\n## Root cause\n\n`installer_server._self_terminate` calls `urllib.request.urlopen` on the\nRunPod DELETE endpoint, then `os._exit(0)`. Two known fail modes for the\nDELETE:\n\n1. **No `User-Agent` header.** REST/proxy quirks observed during 5f2\n   verification: RunPod's HTTP proxy 403s default `Python-urllib/3.x`.\n   REST `/v1/pods` likely behaves similarly for at least some endpoints.\n   The request is wrapped in `try: ... except: pass` so the failure\n   is silent.\n2. Even if DELETE succeeds eventually, **`os._exit(0)` triggers RunPod's\n   container-restart-on-exit behavior** before the DELETE completes\n   (the SHUTDOWN_DELAY_SEC=5 sleep is best-effort).\n\nAfter exit, RunPod restarts the container → entrypoint runs again →\ninstaller_server boots → idle watchdog eventually exits 5 min later →\ncontainer restarts AGAIN. Loop visible in pod logs.\n\n## Fix — move teardown to the orchestrator\n\nThe CLI is the canonical lifecycle owner: it spawned the pod, it has the\nAPI key, it knows the pod_id. Move the DELETE to the CLI side.\n\n### Concrete changes\n\n**comfy_gen/install_preset.py — primary fix:**\n\n- In `run()`, after `shutdown_pod(...)` (or any early failure for a pod\n  this CLI invocation spawned), call `delete_pod(api_key, pod_id)` from\n  the orchestrator. Don't depend on the pod self-terminating.\n- Gate the orchestrator-side DELETE on `spawned_pod=True` so\n  `install-call` mode (caller-managed pod) doesn't accidentally delete\n  someone else's pod.\n- On install failure where the pod should stay alive for log inspection\n  (per the existing edge-case table: `install_error` mid-stream), still\n  skip the orchestrator DELETE. That branch is already wired (`install_ok\n  is False`); just don't call delete_pod in that branch.\n\n**serverless-runtime/installer_server.py — belt:**\n\n- Set `User-Agent: comfygen-installer/0.2` on the in-pod DELETE attempt.\n- Log the result (success/failure + http status) to stderr instead of\n  swallowing silently — so future debugging has a breadcrumb.\n- The `os._exit(0)` line stays (graceful drain) but is no longer\n  load-bearing for billing.\n\n## TDD plan\n\n1. Add a test for `install_preset.run()` happy-path that asserts\n   `delete_pod` is called once with the spawned `pod_id` after a\n   successful install_done.\n2. Add a test that `delete_pod` is **not** called when `--keep-alive`\n   is passed (existing behavior).\n3. Add a test that `delete_pod` is **not** called on `install-call`\n   mode (no spawn — orchestrator didn't own the pod).\n4. Add a test that `delete_pod` is **not** called when install ends\n   with `install_error` (pod stays alive for log inspection).\n5. Live-verify: spawn a pod via the CLI, observe `costPerHr=0.06`,\n   call /shutdown, confirm pod 404s within ~10s AND no `[installer]\n   starting` restart line appears in logs.\n\n## Acceptance\n\n- After a successful `comfy-gen install-preset` run, the spawned pod\n  is gone (`GET /v1/pods/\u003cid\u003e` → 404) within 30s and never restarts.\n- 5+ minute follow-up check (`GET /v1/pods` listing) confirms no\n  rogue installer pod is running.\n- CircleCI smoke run finishes with no leftover installer pod billing.\n- All existing 99 ComfyGen tests still pass; 4 new tests added.\n\n## Blocks\n\n- [[remote_comfy_generator-gnt]] is functionally broken without this:\n  smoke would replace one expensive teardown (GPU worker keeps running\n  until the smoke pod exits the workflow) with a different expensive\n  teardown (CPU installer pod gets restarted by RunPod forever).\n- Sibling consumer work [[sgs-ui-8ww]] (BlockFlow) inherits the same fix\n  for free once the CLI is patched — they're shelling out to the CLI.\n\n## Out of scope\n\n- Removing the in-pod /shutdown endpoint. It's still useful as a clean\n  \"drain\" signal (server stops accepting new installs, finishes the\n  current one, exits Python). The teardown layer is what changes.\n- Adding a global \"delete all orphaned installer pods\" sweep — out of\n  scope for the fix; could be a separate health-check bead later.","status":"closed","priority":1,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-24T08:31:08Z","created_by":"Hearmeman24","updated_at":"2026-05-24T08:53:12Z","started_at":"2026-05-24T08:31:30Z","closed_at":"2026-05-24T08:53:12Z","close_reason":"Live-verified: install_done → /shutdown → orchestrator DELETE → GET /pods/\u003cid\u003e = 404 within 1s, still 404 after 30s, no [installer] starting restart line. Also covers health-timeout path (saw 3 orphans during dev — all now DELETEd by the same code). Belt: in-pod self-DELETE now logs success/HTTP-error/exception to stderr (was silently swallowed) and sets User-Agent: comfygen-installer/0.2. Merged: ComfyGen main @ ae3e0a6, remote-comfy-gen-handler main @ d84f87e. BlockFlow-side safety net tracked at sgs-ui-c7n.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-msf","title":"[Dockerfile] Consume nodes.lock — pin ComfyUI core + every custom node SHA","description":"**Scope:** update serverless-docker/Dockerfile:\n\n1. ComfyUI core clone: 'git clone \u003curl\u003e /ComfyUI \u0026\u0026 git -C /ComfyUI checkout \u003csha-from-lock\u003e'.\n2. Custom node loop: COPY the lockfile into the build context, parse it, for each line clone the repo and checkout the SHA before running requirements.txt/install.py.\n3. Add CI-time assertion: every repo listed in Dockerfile's install array must have a matching entry in nodes.lock (fail build if mismatched).\n\n**Acceptance:**\n- v\u003cN+1\u003e built after lockfile is generated produces an image whose 'git -C /ComfyUI rev-parse HEAD' (and same for each custom_nodes/*) matches nodes.lock exactly.\n- Build still succeeds with the current set of nodes.\n\n**Blocked by:** freeze script must exist first.","status":"closed","priority":1,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T16:47:15Z","created_by":"Hearmeman24","updated_at":"2026-05-23T17:57:59Z","started_at":"2026-05-23T17:53:34Z","closed_at":"2026-05-23T17:57:59Z","close_reason":"Closed","dependencies":[{"issue_id":"remote_comfy_generator-msf","depends_on_id":"remote_comfy_generator-8mq","type":"blocks","created_at":"2026-05-23T19:47:26Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-8mq","title":"[freeze] Script to snapshot local SHAs into serverless-docker/nodes.lock","description":"**Scope:** new script (probably bash or python, at serverless-docker/scripts/freeze.sh or .py) that:\n\n1. Reads the Dockerfile to extract the canonical custom_nodes repo list.\n2. For each repo, reads SHA from ~/src/comfy/ComfyUI/custom_nodes/\u003cdir\u003e/.git (via 'git -C ... rev-parse HEAD') if present.\n3. For Dockerfile-listed repos NOT present locally, falls back to 'git ls-remote \u003curl\u003e HEAD' and warns.\n4. Also captures ComfyUI core SHA from ~/src/comfy/ComfyUI/.git.\n5. Emits serverless-docker/nodes.lock — one line per entry: '\u003crepo_url\u003e \u003csha\u003e' (whitespace-separated, sorted by repo for diff-ability).\n6. WARNS (not fails) on:\n   - Local-only nodes not in Dockerfile (lists them with 'consider adding to Dockerfile if used in workflows')\n   - Dockerfile-listed nodes missing locally (used remote HEAD fallback)\n7. Idempotent — re-running with no local changes produces byte-identical lockfile.\n\n**Acceptance:**\n- 'bash freeze.sh' runs from serverless-docker/ and writes nodes.lock to the same dir.\n- Lockfile format documented in CLAUDE.md.\n- Tests cover: happy path, missing-local node, malformed Dockerfile, .git missing in local node dir.\n\n**Blocked by:** none (foundational).","status":"closed","priority":1,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T16:47:14Z","created_by":"Hearmeman24","updated_at":"2026-05-23T17:57:58Z","started_at":"2026-05-23T17:53:33Z","closed_at":"2026-05-23T17:57:58Z","close_reason":"Closed","dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-vqd","title":"EPIC: Eliminate version-drift errors from releases (local↔docker pin sync)","description":"**Goal:** every workflow you author locally validates against the docker image built from that same moment in time, every time. No more 'Required input is missing' or 'Value not in list' surprises caused by docker pulling a newer node main than your local has.\n\n**Root causes observed in v18/v19:**\n1. Dockerfile uses 'git clone --depth 1' for ComfyUI core + every custom node → each docker build pulls latest main. Two custom nodes (OpenRouterNode, DrawMaskOnImage) added new required inputs in the last ~14 days; workflows authored before that now fail validation.\n2. ONNX dropdown bug in kijai/ComfyUI-WanAnimatePreprocess (issue #32, open) — cache-ordering race in folder_paths. controlnet_aux loads alphabetically before WanAnimatePreprocess and warms the cache without .onnx.\n3. 4 local-only nodes (GGUF, SCAIL-Pose, tensorops, eulerflow-discrete-scheduler) aren't shipped — workflows using them fail at runtime.\n4. 12 docker-only nodes ship but aren't authored against — bloat + cache-ordering risks.\n\n**Design (locked):**\n- **Source of truth:** local → docker. Lockfile generated FROM local, consumed BY docker build.\n- **Granularity:** per-node SHA + ComfyUI core SHA. No python deps freeze (too much churn for now).\n- **Bump trigger:** on-demand. User runs 'freeze' before tagging.\n- **List authority:** Dockerfile canonical for what ships; freeze fills SHAs from local where available, ls-remote main as fallback for docker-only nodes.\n\n**Acceptance:**\n- 'make freeze' (or equivalent) reads local SHAs, writes nodes.lock to serverless-docker, warns on local-only nodes not in Dockerfile.\n- Dockerfile build consumes nodes.lock + checks out exact SHAs (ComfyUI core + every custom node).\n- Smoke gate detects when a workflow's class_types aren't installed in the image — fail early, not at runtime.\n- Documented release flow in CLAUDE.md.\n\n**Cross-references:** docker repo Hearmeman24/remote-comfy-gen-docker; ComfyGen Hearmeman24/ComfyGen; existing CI gate (bead 9ws, closed).","status":"open","priority":1,"issue_type":"feature","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T16:46:28Z","created_by":"Hearmeman24","updated_at":"2026-05-23T16:46:28Z","dependencies":[{"issue_id":"remote_comfy_generator-vqd","depends_on_id":"remote_comfy_generator-4bs","type":"blocks","created_at":"2026-05-23T19:47:29Z","created_by":"Hearmeman24","metadata":"{}"},{"issue_id":"remote_comfy_generator-vqd","depends_on_id":"remote_comfy_generator-6qq","type":"blocks","created_at":"2026-05-23T19:47:30Z","created_by":"Hearmeman24","metadata":"{}"},{"issue_id":"remote_comfy_generator-vqd","depends_on_id":"remote_comfy_generator-8mq","type":"blocks","created_at":"2026-05-23T19:47:27Z","created_by":"Hearmeman24","metadata":"{}"},{"issue_id":"remote_comfy_generator-vqd","depends_on_id":"remote_comfy_generator-dio","type":"blocks","created_at":"2026-05-23T21:53:41Z","created_by":"Hearmeman24","metadata":"{}"},{"issue_id":"remote_comfy_generator-vqd","depends_on_id":"remote_comfy_generator-k5u","type":"blocks","created_at":"2026-05-23T19:47:28Z","created_by":"Hearmeman24","metadata":"{}"},{"issue_id":"remote_comfy_generator-vqd","depends_on_id":"remote_comfy_generator-kv9","type":"blocks","created_at":"2026-05-23T23:19:38Z","created_by":"Hearmeman24","metadata":"{}"},{"issue_id":"remote_comfy_generator-vqd","depends_on_id":"remote_comfy_generator-msf","type":"blocks","created_at":"2026-05-23T19:47:27Z","created_by":"Hearmeman24","metadata":"{}"},{"issue_id":"remote_comfy_generator-vqd","depends_on_id":"remote_comfy_generator-q88","type":"blocks","created_at":"2026-05-23T19:47:28Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":8,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-kz8","title":"comfy_client: 'Partial execution' warning is misleading on normal ComfyUI graph pruning","description":"comfy_client.poll_completion logs WARNING when completed_nodes \u003c nodes_to_execute (computed as all_node_ids - cached_node_ids). But ComfyUI's executor traverses from output sinks backward — branches not connected to a needed sink are correctly skipped. 53/64 in a successful job is normal, not an error. Reframe as INFO with a clearer message, OR suppress when the job ultimately succeeded (history.status.status_str == 'success').","status":"closed","priority":2,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-28T01:03:04Z","created_by":"Hearmeman24","updated_at":"2026-05-28T01:04:52Z","started_at":"2026-05-28T01:03:09Z","closed_at":"2026-05-28T01:04:52Z","close_reason":"shipped in remote-comfy-gen-handler@3d209bd; 4 new tests","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-7r3","title":"Analyze model downloader stall logs","description":"Inspect /Users/avivkaplan/Dump/logs.txt to identify where the recently improved model downloader stalls and report the specific log points.","status":"closed","priority":2,"issue_type":"task","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-27T05:45:08Z","created_by":"Hearmeman24","updated_at":"2026-05-27T05:45:45Z","started_at":"2026-05-27T05:45:14Z","closed_at":"2026-05-27T05:45:45Z","close_reason":"Log analysis completed; stall point identified as CivitAI version 2932204/gonzalomoZpop_v40.safetensors after metadata resolution, before any aria2c progress or completion output.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-96z","title":"CivitAI sha256 dedup: skip download when expected SHA matches existing file","description":"Mirror the URL path's content-addressable dedup (cached:true) for CivitAI sources. CivitAI's GET /api/v1/model-versions/{version_id} response includes per-file hashes.SHA256 — confirmed via Context7. Flow: before running download_with_aria.py, fetch the version metadata, look up files[].hashes.SHA256 for the primary file, scan the dest dir for any file matching that hash, skip subprocess + return cached:true if found. Also lets preset entries declare expected_sha for CivitAI (currently only URL entries can).","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T17:28:46Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:32:43Z","started_at":"2026-05-26T17:29:02Z","closed_at":"2026-05-26T17:32:43Z","close_reason":"CivitAI API sha lookup + content-addressable dedup; mirrors URL cached:true; progress messages now show real filename; covers both GPU /download and CPU install-preset paths; 5 new tests; worker shipped in remote-comfy-gen-handler@badf287","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-poo","title":"download_handler: stream CivitAI subprocess output for live progress","description":"_download_civitai uses subprocess.run(capture_output=True) which buffers stdout/stderr until exit. Multi-GB CivitAI downloads emit nothing to logs for the full duration, then dump everything at end. Mirror _download_url's Popen+line-stream pattern: stream stdout, parse aria2c progress, emit progress_callback events.","status":"closed","priority":2,"issue_type":"task","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T17:21:59Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:24:01Z","started_at":"2026-05-26T17:22:01Z","closed_at":"2026-05-26T17:24:01Z","close_reason":"streamed Popen + per-line print + runpod progress_update throttled 3s + SSE download_progress + 15s heartbeat; 3 new tests; worker change shipped in remote-comfy-gen-handler@12d81df","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-bmq.6","title":"P2 B.3.1: expose volume_root in 'comfy-gen info' output","description":"Add volume_root: '/runpod-volume' (or whatever the worker actually mounts) to the info JSON output. BlockFlow caches it and uses to construct model paths instead of hardcoded /runpod-volume/ComfyUI/models/loras/ (sgs-ui-h1c.1.10).","status":"closed","priority":2,"issue_type":"task","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T16:24:54Z","created_by":"Hearmeman24","updated_at":"2026-05-26T17:12:14Z","closed_at":"2026-05-26T17:12:14Z","close_reason":"info_handler returns volume_root from delete_handler.VOLUME_ROOT; CLI help documents the field; 3 worker-side tests","labels":["audit","blockflow","contract","cross-repo","p2"],"dependencies":[{"issue_id":"remote_comfy_generator-bmq.6","depends_on_id":"remote_comfy_generator-bmq","type":"parent-child","created_at":"2026-05-26T19:24:53Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-ndk","title":"download_handler: defensively split dest when it contains a file path","description":"## Symptom\n\n`download_handler` errored with `FileExistsError` on `os.makedirs(dest_dir, exist_ok=True)` because a caller passed the FULL FILE PATH in `dest` (sgs-ui bead src-b6b is the caller fix).\n\n## Why the dedup couldn't save it\n\nAPI: `dest` is supposed to be a SUBFOLDER; `destination_path` carries full relative path. Misuse → dest_dir is the file itself → target_path = file/filename → `os.path.isfile(target_path)` False → no cache hit → makedirs explodes.\n\n## Defensive fix\n\nIn `_resolve_target` (`download_handler.py:240`), normalize when `dest` looks like a file path: if it contains `/` AND the last segment has an extension, treat it like `destination_path` and split. Caller intent is unambiguous in that case.\n\nPseudo:\n\n```python\ndef _resolve_target(dl):\n    if 'destination_path' in dl and dl['destination_path']:\n        return _split_destination_path(dl['destination_path'])\n    dest = dl.get('dest', 'checkpoints')\n    filename = dl.get('filename')\n    if not filename and '/' in dest and '.' in dest.rsplit('/', 1)[1]:\n        return _split_destination_path(dest)\n    return dest, filename\n```\n\n## Tests\n\n- happy path: `{dest: 'text_encoders/foo.safetensors', source: 'url', ...}` → resolves to dest='text_encoders', filename='foo.safetensors', dedups against existing file, returns cached=True.\n- regression: existing `{dest: 'text_encoders'}` (subfolder only) still works.\n- regression: existing `{destination_path: 'text_encoders/foo.safetensors'}` still works.\n- skip normalization when explicit filename is provided (caller knew what they were doing).","status":"closed","priority":2,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T15:29:30Z","created_by":"Hearmeman24","updated_at":"2026-05-26T15:31:12Z","started_at":"2026-05-26T15:29:34Z","closed_at":"2026-05-26T15:31:12Z","close_reason":"Shipped: handler@dcb20d3 (_resolve_target splits dest when it looks like a file path) + tests@5f8e60c (3 cases incl. prod bug reproduction). 123 tests passing.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-cbg","title":"Legacy download_handler: accept 'huggingface' as a source alias for 'url'","description":"## Background\n\n`/runpod-volume/runtime/download_handler.py:370` rejects any source value other than `civitai` or `url`:\n\n```\nRuntimeError: Download 1: unknown source 'huggingface'. Use 'civitai' or 'url'.\n```\n\nObserved 2026-05-26 when BlockFlow's new GPU-fallback install path (sgs-ui-wx0) shelled out to `comfy-gen download --batch` against a preset whose models declared `source: \"huggingface\"`. Worker request 16cb00c9-7b23-43ac-8732-95f8e9707251-e1 on the comfygen serverless endpoint.\n\nThe new `comfy-gen install-preset` CLI understands `huggingface` natively, but the legacy `comfy-gen download --batch` → worker `download_handler` path does not. This is a schema/worker drift: the blockflow-presets registry schema allows `source: \"huggingface\"` (and existing presets like wan22-svi-4pass use it), but the worker download handler hasn't kept up.\n\n## Why this matters\n\n`huggingface` is, functionally, just an aria2c download against a URL — exactly the same code path as `source: \"url\"`. The handler already does the right thing for `url`; it only needs to treat `huggingface` as an alias. There's no behavior difference to preserve.\n\nThis blocks every caller of `comfy-gen download` (BlockFlow's GPU fallback, manual CLI use, anything driving the legacy serverless download path) on any preset/batch authored against the current schema.\n\n## Fix\n\nIn `download_handler.py` around the source-dispatch switch (line ~370), accept `huggingface` and route it to the same handler as `url`. Either:\n- add `huggingface` to the accepted-sources set with the same dispatch as `url`, OR\n- normalize the incoming `source` field at entry: `if source == \"huggingface\": source = \"url\"`.\n\nSlight preference for the alias approach (preserves the original `source` for logging/telemetry if any downstream code cares).\n\n## Test plan\n\n1. Unit test in `tests/test_download_handler.py`: feed the handler a batch entry with `source: \"huggingface\"` + a known-good URL and assert it dispatches to the URL/aria2c path (not RuntimeError).\n2. Mirror the existing url-path success test with `huggingface` substituted; confirm the resulting download call shape is identical.\n3. Regression: existing `civitai` / `url` tests stay green.\n\n## Live verification\n\nRe-run the BlockFlow GPU fallback install (sgs-ui-wx0) against wan22-svi-4pass (or any HF-sourced preset) → expect the worker to download all files instead of failing on entry #1.\n\n## Discovered from\n\nBlockFlow sgs-ui-wx0 live test 2026-05-26. Full error payload captured in the conversation that filed this bead.","status":"closed","priority":2,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T15:13:56Z","created_by":"Hearmeman24","updated_at":"2026-05-26T15:16:31Z","started_at":"2026-05-26T15:14:28Z","closed_at":"2026-05-26T15:16:31Z","close_reason":"Shipped: handler@dcc3490 normalizes source='huggingface' → 'url' at entry; tests@38f53e7 cover happy + dedup paths. 120 tests passing.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-0xm","title":"Pin CivitAI_Downloader SHA in serverless-docker Dockerfile","description":"The Hearmeman24/CivitAI_Downloader clone in serverless-docker/Dockerfile is unpinned (--depth 1 from main), so the image is non-deterministic and Docker layer caching can serve a stale clone. Pin via ARG CIVITAI_DL_SHA matching nodes.lock discipline, then tag v24 to ship the latest CivitAI_Downloader update.","status":"closed","priority":2,"issue_type":"task","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-26T14:13:50Z","created_by":"Hearmeman24","updated_at":"2026-05-26T14:16:23Z","closed_at":"2026-05-26T14:16:23Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-gnt","title":"Migrate CircleCI smoke_preset.py from GPU comfy-gen download to CPU install-preset","description":"# Migrate CircleCI smoke from `comfy-gen download` (GPU) to `comfy-gen install-preset` (CPU pod)\n\nCloses the CI half of the cost-cut started in [[remote_comfy_generator-5f2]].\n\n## Rationale\n\n`automation/smoke_preset.py` (called by CircleCI's `smoke_test` job per preset\non every `v\u003cN\u003e` tag) still uses the pre-5f2 path:\n\n```python\n# smoke_preset.py:148 — current\nsubprocess.run([\n    \"comfy-gen\", \"download\",\n    \"--batch\", dl_file,\n    \"--endpoint-id\", ENDPOINT_ID,  # 5fkobbksgrtww6 — GPU serverless\n    ...\n])\n```\n\nThat dispatches the model download to the **GPU serverless worker** — burns\nroughly $1.50 of H100 time per preset per release tag, just to run `aria2c`.\nNow that `comfy-gen install-preset` (bead 5f2) is on `main` and ships the\nCPU-pod path end-to-end, smoke should use it too. Same volume gets the same\nfiles — the only difference is which kind of pod does the writing.\n\nCost win: ~$1.50 → ~$0.005 per preset per `v\u003cN\u003e` tag (≈300× cheaper). With\n2 presets in the live manifest today (`qwen-image-lighting`, `wan-animate`)\nthat's $3 → $0.01 per release.\n\n## What changes\n\n### `automation/smoke_preset.py`\n\n1. Replace the `comfy-gen download --batch` block (around line 143-160) with\n   a `comfy-gen install-preset --preset-id \u003cid\u003e --volume-id \u003cvid\u003e` invocation.\n2. Drop `build_downloads(preset)` (line 82) — `install-preset` resolves the\n   preset manifest itself via `preset_resolver`.\n3. Drop the `/tmp/smoke-\u003cid\u003e-downloads.json` batch file path entirely.\n4. Add a helper `resolve_volume_for_endpoint(endpoint_id) -\u003e str` that does\n   `GET https://rest.runpod.io/v1/endpoints/\u003cep\u003e` and returns the first\n   `networkVolumeIds[0]`. Cache in module-global so it runs once per smoke\n   job.\n5. Parse the new stdout — `install-preset` emits **line-delimited JSON\n   events**, not one final JSON blob. The existing `_run_comfygen(...)` helper\n   (line 101) returns parsed-JSON-stdout assuming the old shape; either:\n   - branch on subcommand inside `_run_comfygen`, **or**\n   - write a small `_run_install_preset(...)` helper that consumes the event\n     stream line-by-line, asserts `install_done.ok == true`, and returns the\n     `files` array from the final event.\n6. Keep the summary log line shape — `{downloads: {total, cached, fresh}}`\n   is reconstructable from the `download_done` events (count `cached: true`\n   vs `cached: false`).\n\n### `serverless-docker/.circleci/generate_continue.py`\n\nNo required change. `smoke_test` already runs `install-comfygen` (clones\n`main` and `pip install`s), so the new `install-preset` subcommand will be\navailable after this bead lands on ComfyGen `main`. But:\n\n- Remove the FlashBoot-only comment in `smoke_test`'s description if it\n  implies \"uses the worker to download\" — the only thing GPU-serverless\n  does in smoke now is execute the workflow (which still needs a worker).\n- Optional doc tweak: the smoke_test job no longer waits on\n  `wait_for_rollout` solely to verify the worker can run aria2c; it now\n  only depends on the worker being up to *run the workflow*.\n\n### Maybe needed: smoke `--volume-id` arg\n\n`smoke_preset.py` only takes `--endpoint-id` today. Two options:\n\n- **A (recommended):** resolve volume from endpoint inside the script. One\n  REST call. Zero changes to CircleCI config.\n- **B:** add `--volume-id` to `smoke_preset.py` and hard-code it in\n  `generate_continue.py` next to `ENDPOINT_ID`. Slightly faster (no REST\n  hop) but adds a second magic constant that drifts.\n\nA unless you have a reason.\n\n## TDD plan\n\n### Task 1 — `_run_install_preset` helper\n\n- Failing test in `tests/test_smoke_preset.py` (it exists per the\n  serverless-runtime test list): mock `subprocess.Popen` to yield a\n  canned `install-preset` event stream → helper returns the file list +\n  cached/fresh counts.\n- Failing test: stream contains `install_error` → helper raises with the\n  reason.\n- Failing test: stream contains `preflight_fail` → helper raises with the\n  reason (no `install_done` event ever arrives).\n- Failing test: subprocess exit 1 with no terminal event → helper raises\n  \"unexpected exit\".\n- Implement the helper, run tests, expect pass.\n\n### Task 2 — `resolve_volume_for_endpoint`\n\n- Failing test: mocks `urllib.request.urlopen` returning an endpoint with\n  `networkVolumeIds: [\"abc\"]` → returns `\"abc\"`.\n- Failing test: endpoint has empty `networkVolumeIds` → raises with a\n  clear message (\"endpoint has no network volume attached\").\n- Failing test: endpoint has \u003e1 volume → returns the first one + logs a\n  warning to stderr.\n- Implement, expect pass.\n\n### Task 3 — wire into the main flow\n\n- Replace the `comfy-gen download` block with `_run_install_preset(...)`.\n- Drop `build_downloads`, drop `/tmp/smoke-*-downloads.json`.\n- Failing integration test (still mocked at subprocess boundary) that\n  drives `main(preset_id=\"qwen-image-lighting\", endpoint_id=\"...\")`\n  through happy path: stream consumed → workflow submitted → success.\n\n### Task 4 — live verification\n\n- Tag `v\u003cN+1\u003e` on `serverless-docker` after this bead's PR is on main\n  (small, no-op image change is fine; the purpose is to fire the smoke\n  matrix and verify cost moves from ~$1.50/preset to ~$0.005/preset).\n- Confirm in CircleCI logs that the smoke step shows `pod_spawned` and\n  `install_done` events, not `Downloading 1/N` lines.\n- Confirm in the RunPod billing dashboard that the spawned pod was a\n  CPU pod (`costPerHr` ≈ $0.06).\n\n## Acceptance\n\n- `automation/smoke_preset.py` no longer calls `comfy-gen download`.\n- New helper consumes line-delimited JSON events.\n- Test suite in `tests/test_smoke_preset.py` covers happy path + 3 error\n  paths + volume resolution edge cases.\n- One `v\u003cN\u003e` smoke run on `main` completes and shows CPU pod cost per\n  preset.\n- Smoke runtime per preset stays within ~5 min (CPU pod download is\n  parallelizable per file via aria2c, same as before — but no rollout\n  warm-up cost).\n\n## Out of scope\n\n- Migrating BlockFlow's `preset_routes.py` install path — tracked in\n  [[sgs-ui-8ww]].\n- Adding GPU fallback when no CPU capacity. The CLI fails loudly today;\n  if CI starts seeing capacity failures in practice we'll add fallback\n  in the CLI itself.\n- Per-volume parallel install (smoke is one-volume-per-endpoint by\n  construction).\n\n## Dependencies\n\n- **Blocks on:** [[remote_comfy_generator-5f2]] merged to main on all\n  three repos. **Done as of 2026-05-24.**\n- After this lands, `serverless-docker/.circleci/generate_continue.py`\n  doesn't need a tag bump in lockstep — `smoke_test` clones ComfyGen\n  `main` at job start, so the new behavior ships the moment this bead's\n  PR is on ComfyGen main.","status":"closed","priority":2,"issue_type":"task","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-24T08:23:45Z","created_by":"Hearmeman24","updated_at":"2026-05-24T08:53:13Z","started_at":"2026-05-24T08:26:29Z","closed_at":"2026-05-24T08:53:13Z","close_reason":"Replaced comfy-gen download --batch (GPU serverless, ~$1.50/preset) with comfy-gen install-preset (CPU pod, ~$0.005/preset) in automation/smoke_preset.py. Three new helpers: resolve_volume_for_endpoint, run_install_preset (line-delimited JSON event stream consumer), and the integration into smoke(). 15/15 tests pass in test_smoke_preset.py (8 new); project-wide 108/108. Merged: ComfyGen main @ ae3e0a6. CircleCI smoke job needs no config changes — it already pip-installs ComfyGen main on every run.","dependencies":[{"issue_id":"remote_comfy_generator-gnt","depends_on_id":"remote_comfy_generator-5f2","type":"blocks","created_at":"2026-05-24T11:23:51Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-5f2","title":"Installer pod: addressable HTTP server (port 3000) with SSE progress + multi-op API","description":"# Installer pod: addressable HTTP server with SSE progress\n\n\u003e **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.\n\u003e TDD doctrine in bd memories applies — failing test first, real behavior in tests, edge cases mandatory.\n\u003e Builds on [[remote_comfy_generator-ixd]] (shipped 2026-05-24 — one-shot CPU installer pod).\n\n**Goal:** Convert the installer pod from a one-shot `PRESET_ID`-env batch job into an addressable mini-service on port 3000. BlockFlow spawns the pod, waits for `/health`, then drives it via REST (preflight → install → shutdown) and streams progress via SSE — eliminating the \"no log API\" problem from ixd entirely.\n\n**Why now:** ixd shipped successfully (qwen-install in 56s for $0.0019) but BlockFlow has no way to surface progress to the user — RunPod has no pod-logs API, only a dashboard URL. An in-pod HTTP server gives BlockFlow a real progress stream, lets it preflight free space before committing, and supports multi-op pods (install preset A, then B, without paying two cold starts).\n\n---\n\n## Decision tree (locked via session 2026-05-24)\n\n| Q | Decision | Why |\n|---|---|---|\n| Where does the server live | Inside the existing `serverless-runtime` repo as `installer_server.py` — shipped via the same `git clone` the pod already does on boot | One source of truth, no new repo, no Dockerfile changes needed for server logic updates |\n| Port | 3000, exposed via RunPod's HTTP proxy at `https://\u003cpod_id\u003e-3000.proxy.runpod.net/` | RunPod's standard proxy pattern; HTTPS terminated by RunPod |\n| Auth | `X-Installer-Token` header, secret generated at pod-spawn time and injected as `INSTALLER_TOKEN` env | RunPod's proxy is unauthenticated by default; anyone with the URL can hit the server otherwise |\n| Web framework | aiohttp (stdlib-adjacent, async-native SSE, no codegen) | FastAPI brings pydantic+uvicorn; overkill for ~4 endpoints. aiohttp is one pip install |\n| Backward compat | Keep one-shot env-driven mode from ixd as fallback when `INSTALLER_MODE=oneshot` or `PRESET_ID` is set at spawn | Preserves ixd success path for CI/cron use cases |\n| Default mode | Server mode (no `PRESET_ID` at spawn = boot server) | New behavior is the primary; ixd-style is the escape hatch |\n| Watchdog | Server self-terminates if no request in 5 min (configurable via `INSTALLER_IDLE_TIMEOUT_SEC`) | Prevents abandoned pods from idle-billing forever if BlockFlow crashes between spawn and first call |\n| Image | Reuse `Dockerfile.installer`, add `aiohttp` to pip install | No new image needed — same `installer-v\u003cN\u003e` tag pattern |\n\n---\n\n## File structure\n\n### NEW: `serverless-runtime/installer_server.py`\n~150 lines. aiohttp app with these endpoints:\n\n| Method | Path | Auth | Body | Response |\n|---|---|---|---|---|\n| GET | `/health` | none | — | `{\"ok\": true, \"version\": \"\u003cpyproject\u003e\", \"ready\": true}` |\n| GET | `/volume_info` | token | — | Delegates to `volume_info_handler.handle()` |\n| POST | `/install/{preset_id}` | token | `{\"civitai_token\": \"\u003copt\u003e\", \"hf_token\": \"\u003copt\u003e\"}` | **SSE stream** of `{type, ...}` events |\n| POST | `/shutdown` | token | — | `{\"ok\": true, \"terminating\": true}` then DELETEs self if `RUNPOD_API_KEY` env set |\n\n**SSE event shapes (one event per line, `data: \u003cjson\u003e\\n\\n`):**\n```jsonc\n{\"type\": \"preflight_start\"}\n{\"type\": \"preflight_ok\", \"preset_id\": \"...\", \"models_count\": 4, \"total_bytes\": 52000000000, \"volume_free_bytes\": 800000000000}\n{\"type\": \"preflight_fail\", \"reason\": \"...\"}                  // insufficient space, preset not found\n{\"type\": \"download_start\", \"file_index\": 0, \"file\": \"...\", \"bytes\": 40861031488}\n{\"type\": \"download_progress\", \"file_index\": 0, \"percent\": 45, \"speed_bytes_sec\": 52428800}\n{\"type\": \"download_done\", \"file_index\": 0, \"cached\": false, \"sha256\": \"...\"}\n{\"type\": \"install_done\", \"ok\": true, \"files\": [...], \"elapsed_sec\": 56}\n{\"type\": \"install_error\", \"reason\": \"...\", \"stage\": \"...\"}   // any handler exception\n```\n\n**Reuses existing handler logic:**\n- `/volume_info` → calls `volume_info_handler.handle({\"input\": {\"command\": \"volume_info\"}})`\n- `/install/{preset_id}` → fetches manifest + preset.json (same code currently in `install-entrypoint.sh`, **lift into a Python module** `serverless-runtime/preset_resolver.py` so both the server and the legacy oneshot mode share it), then invokes `download_handler.handle()` but with a progress-callback hook (see next section)\n\n### MODIFY: `serverless-runtime/download_handler.py`\nAdd an optional `progress_callback: Callable[[dict], None] = None` parameter to `handle()` that, when set, is invoked with structured events instead of (or in addition to) `runpod.serverless.progress_update`. The aiohttp server passes a callback that puts events onto an `asyncio.Queue` consumed by the SSE generator.\n\n```python\ndef handle(job: dict, progress_callback: Callable[[dict], None] | None = None) -\u003e dict:\n    ...\n    # at each progress point:\n    if progress_callback:\n        progress_callback({\"type\": \"download_progress\", \"file_index\": i, \"percent\": pct, ...})\n    else:\n        _send_progress(job, ...)  # legacy harness path\n```\n\n**Backwards compatibility:** all existing callers (worker dispatch, CLI mode, tests) pass no callback → behavior unchanged. All 16 existing download_handler tests must continue to pass.\n\n### NEW: `serverless-runtime/preset_resolver.py`\n~30 lines. Extracted from `install-entrypoint.sh`:\n```python\ndef resolve_preset(preset_id: str, manifest_url: str = DEFAULT_MANIFEST_URL) -\u003e dict:\n    \"\"\"Fetch manifest, look up preset_id, fetch preset.json, return parsed preset dict.\"\"\"\n\ndef preset_to_download_batch(preset: dict) -\u003e list[dict]:\n    \"\"\"Translate preset.models into the download_handler batch shape.\"\"\"\n```\n\n### MODIFY: `serverless-docker/Dockerfile.installer`\nAdd `aiohttp` to the pip install line. No other changes.\n\n### MODIFY: `serverless-docker/install-entrypoint.sh`\nBranch on mode:\n```bash\nif [ -n \"${PRESET_ID:-}\" ] || [ \"${INSTALLER_MODE:-server}\" = \"oneshot\" ]; then\n    # Existing ixd one-shot path — unchanged\n    ...\nelse\n    # New server mode\n    exec python -m installer_server --port 3000 --token \"$INSTALLER_TOKEN\"\nfi\n```\n\n### NEW: ComfyGen CLI command `install-preset`\n`comfy_gen/cli/install_preset.py` — wraps the spawn/poll/install/shutdown dance.\n\n```\ncomfy-gen install-preset --preset-id \u003cid\u003e --volume-id \u003cvid\u003e [--keep-alive] [--image installer-v4]\n  # Spawns pod, waits for /health, calls /install, streams SSE to stdout as JSON lines,\n  # then POSTs /shutdown (unless --keep-alive). Returns 0 on install_done.ok, 1 otherwise.\n\ncomfy-gen install-call --pod-id \u003cpid\u003e --token \u003ct\u003e --preset-id \u003cid\u003e\n  # For BlockFlow's multi-op flow: pod already exists, just call /install and stream.\n```\n\nStdout shape (line-delimited JSON, one event per line — same shape as SSE events). BlockFlow can pipe stdout into its UI directly.\n\n### MODIFY: ComfyGen `tests/` — new test files\n- `tests/test_installer_server.py` — exercises the server with a mocked download_handler. Real aiohttp test client.\n- `tests/test_preset_resolver.py` — mocks urlopen, asserts manifest+preset.json parsing.\n- `tests/test_download_handler.py` — add cases for `progress_callback` parameter (3 new tests; existing 16 must still pass).\n- `tests/test_install_preset_cli.py` — mocks RunPod REST + the pod's HTTP server, drives the full CLI flow.\n\n---\n\n## TDD plan — task-by-task\n\n### Task 1: progress_callback hook in download_handler\n**Repo:** remote-comfy-gen-handler (`serverless-runtime/`)\n- [ ] Write 3 failing tests for `download_handler.handle(job, progress_callback=cb)`:\n  1. callback receives `download_start` before aria2c invoked\n  2. callback receives `download_done` after sha256 verify with `{cached, sha256}`\n  3. callback NOT called when not provided (existing behavior — already covered by 16 existing tests, just re-verify)\n- [ ] Run pytest, expect failures\n- [ ] Add `progress_callback` parameter, sprinkle callback invocations alongside existing `_send_progress` calls\n- [ ] All 19 tests (16 + 3) pass\n- [ ] Commit\n\n### Task 2: preset_resolver module\n**Repo:** remote-comfy-gen-handler\n- [ ] Write failing tests for `resolve_preset` and `preset_to_download_batch`:\n  1. happy path: mocked urlopen returns manifest+preset → returns parsed preset\n  2. preset_id not in manifest → raises `KeyError`\n  3. malformed manifest JSON → raises\n  4. preset_to_download_batch translates `{models: [{url, dest, sha256}]}` → handler batch shape\n- [ ] Implement, lift the logic out of `install-entrypoint.sh` verbatim\n- [ ] Tests pass\n- [ ] **Verify the legacy one-shot entrypoint still works** — bash dry-run with mocked curl (or just trust that the resolver returns the same dict the inline python did)\n- [ ] Commit\n\n### Task 3: installer_server\n**Repo:** remote-comfy-gen-handler\n- [ ] Write failing tests using aiohttp's test client:\n  1. `GET /health` returns `{ok: true, version, ready: true}` with no auth\n  2. `GET /volume_info` without token → 401\n  3. `GET /volume_info` with valid token → delegates to handler\n  4. `POST /install/qwen-image-lighting` with token → SSE stream emits preflight_ok → download_start → download_done → install_done\n  5. `POST /install/missing-preset` with token → SSE emits `preflight_fail`\n  6. `POST /shutdown` with token → returns ok, schedules self-DELETE\n  7. Idle watchdog: no request for `INSTALLER_IDLE_TIMEOUT_SEC=1` → server exits\n  8. download_handler raises mid-install → SSE emits `install_error` then closes stream\n- [ ] Implement the server\n- [ ] Tests pass\n- [ ] Commit\n\n### Task 4: Dockerfile + entrypoint mode branching\n**Repo:** remote-comfy-gen-docker\n- [ ] Add `aiohttp` to `pip install` line in Dockerfile.installer\n- [ ] Modify `install-entrypoint.sh` to branch on PRESET_ID / INSTALLER_MODE\n- [ ] Local bash test: with PRESET_ID set, takes the oneshot path; without, would try to run installer_server (will fail because /runtime not cloned, but the branch decision must be observable from `echo`s)\n- [ ] Generate `installer-v5` tag, push, wait for CircleCI green\n- [ ] Commit\n\n### Task 5: ComfyGen `install-preset` CLI\n**Repo:** ComfyGen (this one)\n- [ ] Write failing tests (mocked RunPod REST + mocked pod HTTP server):\n  1. happy path: spawn → poll /health (3 retries) → POST /install → SSE stream prints to stdout as JSON lines → /shutdown → exit 0\n  2. /health never ready in timeout → exit 1 with clear error\n  3. install_error event → CLI exits 1 with the reason\n  4. --keep-alive flag → skips final /shutdown call\n  5. install-call command (no spawn) → just streams from existing pod\n- [ ] Implement\n- [ ] Tests pass\n- [ ] Update help text per CLAUDE.md \"Every CLI command MUST have verbose, detailed --help output\"\n- [ ] Commit\n\n### Task 6: Live verification\n**Steps:**\n- [ ] Build `installer-v5` (Dockerfile + entrypoint changes via Task 4 tag push)\n- [ ] Spawn a server-mode pod manually via REST, no PRESET_ID env\n- [ ] curl `https://\u003cpod\u003e-3000.proxy.runpod.net/health` → 200\n- [ ] curl `/volume_info` with `X-Installer-Token` → free bytes\n- [ ] curl `-N POST /install/qwen-image-lighting` → stream of SSE events\n- [ ] curl `POST /shutdown` → pod 404s within ~5s\n- [ ] Run `comfy-gen install-preset --preset-id qwen-image-lighting --volume-id 7etzak7vfp` end-to-end\n- [ ] Record numbers: wall time, cold-start window (spawn → /health ready), install time, total cost\n- [ ] Close bead with the numbers in notes\n\n---\n\n## Done criteria\n\n- 3 new `download_handler` tests pass + all 16 existing still pass\n- 4 `preset_resolver` tests pass\n- 8 `installer_server` tests pass\n- 5 `install-preset` CLI tests pass\n- `installer-v5` image builds clean and routes correctly on PRESET_ID presence\n- Live: `comfy-gen install-preset --preset-id qwen-image-lighting --volume-id 7etzak7vfp` runs end-to-end, prints SSE events as JSON lines, exits 0, pod 404s\n- Total wall time for cached install \u003c 90s (60s ixd baseline + ~30s server boot)\n- Total cost \u003c $0.01\n\n---\n\n## Edge cases (per project TDD doctrine — non-negotiable)\n\n| Edge case | Expected behavior |\n|---|---|\n| Token missing in request | 401 with `{ok: false, reason: \"missing X-Installer-Token\"}` |\n| Token wrong | 401 same shape |\n| Two concurrent `/install` calls on the same pod | Second 409s with `{reason: \"install already in progress\"}` |\n| Preset has no models (workflow-only) | install_done with `files: []`, ok: true |\n| Insufficient free space | preflight_fail with `{reason: \"need X bytes, have Y\"}`; no aria2c invoked |\n| aria2c fails mid-download | install_error with stage=\"download\", file_index, partial bytes; pod stays alive so logs are inspectable |\n| SHA mismatch | install_error with stage=\"verify\"; corrupt file removed (existing behavior); pod stays alive |\n| RunPod proxy returns 502 (cold-boot race) | CLI retries `/health` up to N times before failing |\n| Pod self-DELETE fails (RUNPOD_API_KEY missing) | server still exits cleanly; caller responsible for DELETE |\n| Idle watchdog fires mid-install | watchdog must NOT fire while a request is in-flight — gate on a \"request in progress\" flag |\n| Client disconnects mid-SSE | server logs disconnect; install continues to completion (don't cancel a multi-GB download because the client tab closed) |\n| Same pod, /install called twice serially (multi-op) | both succeed; second one's preflight sees cached files from the first |\n\n---\n\n## BlockFlow integration shape (informational — implementation lives in BlockFlow repo)\n\nToday's preset install row gains:\n1. **\"Install\" button** → BlockFlow backend shells `comfy-gen install-preset --preset-id \u003cid\u003e --volume-id \u003cvid\u003e`, captures stdout (one JSON line per SSE event), pushes events to the browser via existing websocket/SSE channel\n2. **Progress bar** — driven by the `download_progress` events (per-file percent + overall progress)\n3. **\"View live logs ↗\" link** — `https://console.runpod.io/pods?id=\u003cpod_id\u003e\u0026inspectorTab=logs` (still useful as escape hatch; the pod_id comes from the spawn response, which CLI can emit as the first stdout line)\n4. **On `install_error`**: show error, leave \"View logs\" link active, pod stays alive (per edge-case table) so the user can debug\n5. **On `install_done`**: pod self-terminates ~5s after, UI flips to \"Installed\"\n\n---\n\n## Cross-references\n\n- **Builds on:** [[remote_comfy_generator-ixd]] (shipped — one-shot pod, the baseline)\n- **Related:** sgs-ui-ngz (BlockFlow's UI work — depends on this for the SSE event shape)\n- **Won't break:** all existing GPU-worker commands (download/list/hash/delete/health/volume_info/object_info) — server only adds, doesn't replace","notes":"Implementation landed on feature branches across all 3 repos (not yet merged to main):\n\n- ComfyGen: feat/installer-server-remote_comfy_generator-5f2\n  https://github.com/Hearmeman24/ComfyGen/pull/new/feat/installer-server-remote_comfy_generator-5f2\n- remote-comfy-gen-handler (serverless-runtime): same branch name\n- remote-comfy-gen-docker (serverless-docker): same branch name\n\nTasks 1-5 complete; Task 6 (live verification) DEFERRED — requires manual\nRunPod operation. Image tag bump (v5) also DEFERRED until live verification\npasses; tagging the docker repo with v5 will trigger CircleCI.\n\nTest counts: 99/99 ComfyGen tests pass (up from 76). Breakdown of new tests:\n- download_handler.progress_callback: 3\n- preset_resolver: 5\n- installer_server (aiohttp): 10\n- install-preset / install-call CLI: 8 (mocks at install_preset module HTTP boundary)\n\nSkipped from the original plan:\n- live curl run against a spawned pod (Task 6)\n- 'installer-v5' image build (gated on Task 6)\n- BlockFlow UI integration (lives in BlockFlow repo, out of scope)\n\nDone criteria still pending: 'comfy-gen install-preset --preset-id qwen-image-lighting'\nend-to-end live run with timing/cost numbers.","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-24T05:55:21Z","created_by":"Hearmeman24","updated_at":"2026-05-24T07:10:07Z","started_at":"2026-05-24T05:56:14Z","closed_at":"2026-05-24T07:10:07Z","close_reason":"Live-verified end-to-end and merged to main on all three repos.\n\nLive results (2026-05-24):\n- CPU pod cost: $0.06/hr (verified via GraphQL deployCpuPod path)\n- Full SSE event sequence captured: pod_spawned -\u003e preflight_start -\u003e\n  preflight_ok -\u003e 4x (download_start + download_done) -\u003e install_done.ok=true\n- Total elapsed (all 4 files cached): 208s on the install-preset CLI flow\n- /shutdown self-terminate verified: pod 404 within ~10s\n- Concurrent /install correctly returns 409\n- Total verification cost: well under $0.10\n\nMerged to main:\n- Hearmeman24/ComfyGen @ 2f08022\n- Hearmeman24/remote-comfy-gen-handler @ 9d88592\n- Hearmeman24/remote-comfy-gen-docker @ 9b6f852 (image installer-v5 already\n  built by CircleCI from the feature-branch tag push)\n\nFinal test count on ComfyGen main: 99/99 pass.\n\nFollow-up consumer work tracked at [[sgs-ui-8ww]] (replaces the old\n[[sgs-ui-ngz]]): wire BlockFlow's preset installer to shell out to\n`comfy-gen install-preset` instead of `comfy-gen download --batch`.\n\nBrain pages filed: concept/runpod-cpu-pod-gotchas (global), projects/\ncomfy-gen.md updated (global), gotcha/installer-pod-modes (project).\nArchitecture overview pages still stale; tracked for a focused\n/brain-sync follow-up.","dependencies":[{"issue_id":"remote_comfy_generator-5f2","depends_on_id":"remote_comfy_generator-ixd","type":"blocks","created_at":"2026-05-24T08:55:31Z","created_by":"Hearmeman24","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-ixd","title":"CPU installer pod image (Dockerfile.installer) + download_handler CLI entrypoint","description":"# CPU installer pod image + download_handler CLI entrypoint\n\n\u003e **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox syntax. **TDD doctrine in bd memories applies — failing test first, real behavior in tests, edge cases mandatory.**\n\n**Goal:** Eliminate ~$1.50 of GPU billing per wan-animate-sized preset install by routing the download work to a CPU pod instead of the GPU serverless worker. ComfyGen ships a one-shot CPU image; BlockFlow (sgs-ui-ngz) spawns it.\n\n**Architecture:** Tiny `python:3.12-slim` image (~200 MB) with `aria2c + git`. Entrypoint git-clones `serverless-runtime` at boot (same pattern as the GPU image), invokes the existing `download_handler.py` via a new `python -m download_handler` CLI mode. Single source of truth for download logic — same sha256 verify, same aria2c invocation, same dedup as the worker.\n\n---\n\n## Decision tree (locked via session 2026-05-24 grill)\n\n| Q | Decision | Why |\n|---|---|---|\n| Where install code runs | Reuse `serverless-runtime/download_handler.py` via NEW `if __name__ == \"__main__\"` CLI | Single source of truth; bug fixes propagate to both worker AND CPU pod |\n| Pod lifecycle | One-shot — CMD runs install, exits with handler's status | RunPod stops billing on pod exit; BlockFlow polls exit code for success/fail |\n| Trigger | BlockFlow installer spawns per network volume | Multi-DC endpoints need same files on every volume |\n| Image home | Second Dockerfile in this repo (`Dockerfile.installer`) | Single repo, single CircleCI project; tag prefix `installer-vN` differentiates from worker `vN` |\n| Reporting | RunPod's pod-log API (stdout) | No new infra; BlockFlow polls `/pods/\u003cid\u003e/logs` |\n\n---\n\n## File structure\n\n### NEW: `serverless-docker/Dockerfile.installer`\n```dockerfile\n# syntax=docker/dockerfile:1.7\nFROM python:3.12-slim\n\nENV DEBIAN_FRONTEND=noninteractive \\\n    PYTHONDONTWRITEBYTECODE=1 \\\n    PYTHONUNBUFFERED=1\n\nRUN --mount=type=cache,target=/var/cache/apt,sharing=locked \\\n    --mount=type=cache,target=/var/lib/apt,sharing=locked \\\n    rm -f /etc/apt/apt.conf.d/docker-clean \u0026\u0026 \\\n    apt-get update \u0026\u0026 apt-get install -y --no-install-recommends \\\n    aria2 git ca-certificates curl\n\n# No torch, no ComfyUI, no custom nodes. Just python + aria2c + git.\nRUN --mount=type=cache,target=/root/.cache/pip,sharing=locked \\\n    pip install --upgrade pip \u0026\u0026 \\\n    pip install runpod  # download_handler imports runpod for progress; install only\n\nCOPY install-entrypoint.sh /install-entrypoint.sh\nRUN chmod +x /install-entrypoint.sh\n\nCMD [\"/install-entrypoint.sh\"]\n```\n\n### NEW: `serverless-docker/install-entrypoint.sh`\n```bash\n#!/bin/bash\nset -euo pipefail\n\necho \"[installer] starting\"\n\n: \"${PRESET_ID:?env var required}\"\n: \"${PRESET_REGISTRY_MANIFEST:=https://raw.githubusercontent.com/Hearmeman24/blockflow-presets/main/manifest.json}\"\n: \"${RUNTIME_REPO_URL:=https://github.com/Hearmeman24/remote-comfy-gen-handler.git}\"\n: \"${RUNTIME_REPO_REF:=main}\"\n\n# CIVITAI_TOKEN, HF_TOKEN — optional, passed via env\n# RUNPOD_API_KEY — not used by installer itself\n\n# Verify volume is mounted + writable\nif [ ! -d /runpod-volume ]; then\n    echo \"[installer] FATAL: /runpod-volume not mounted\"\n    exit 2\nfi\nif ! touch /runpod-volume/.installer-write-test 2\u003e/dev/null; then\n    echo \"[installer] FATAL: /runpod-volume is not writable\"\n    exit 2\nfi\nrm -f /runpod-volume/.installer-write-test\n\n# Clone runtime (same pattern as the GPU image's start_script.sh)\ngit clone --depth 1 --branch \"$RUNTIME_REPO_REF\" \"$RUNTIME_REPO_URL\" /runtime\necho \"[installer] runtime cloned at $(git -C /runtime rev-parse HEAD)\"\n\n# Fetch manifest + preset.json\necho \"[installer] fetching preset $PRESET_ID\"\nPRESET_URL=$(curl -fsSL \"$PRESET_REGISTRY_MANIFEST\" \\\n    | python3 -c \"import json,sys; m=json.load(sys.stdin); print(next(p['preset_url'] for p in m['presets'] if p['id']==sys.argv[1]))\" \"$PRESET_ID\")\nPRESET_JSON=$(curl -fsSL \"$PRESET_URL\")\n\n# Translate preset.models → download_handler batch\necho \"$PRESET_JSON\" | python3 -c \"\nimport json,sys\npreset = json.load(sys.stdin)\nbatch = [{'source':'url','url':m['url'],'destination_path':m['dest'],'sha256':m['sha256']} for m in preset.get('models',[])]\nprint(json.dumps({'input':{'command':'download','downloads':batch}}))\n\" \u003e /tmp/job.json\n\n# Run download_handler in CLI mode\ncd /runtime\nexec python -m download_handler --job /tmp/job.json\n```\n\n### MODIFY: `serverless-runtime/download_handler.py`\nAdd at the bottom:\n```python\ndef _cli_main(argv: list[str] | None = None) -\u003e int:\n    \"\"\"CLI entrypoint for the CPU installer pod.\n\n    Reads a job dict (same shape as the worker dispatch input) from --job FILE\n    or stdin, runs handle(), prints the result as JSON, returns 0 on\n    {ok: true}, else 1.\n    \"\"\"\n    import argparse, sys\n    p = argparse.ArgumentParser(description=\"Download handler — CLI mode for CPU installer pod\")\n    p.add_argument(\"--job\", help=\"Path to job JSON (omit to read stdin)\")\n    args = p.parse_args(argv)\n    src = open(args.job) if args.job else sys.stdin\n    job = json.load(src)\n    if args.job:\n        src.close()\n    result = handle(job)\n    print(json.dumps(result))\n    return 0 if result.get(\"ok\") else 1\n\n\nif __name__ == \"__main__\":\n    import sys\n    sys.exit(_cli_main())\n```\n\nPlus: `_send_progress` currently calls `runpod.serverless.progress_update`. When run via CLI (no runpod harness), that call may raise — current code already has a try/except around it (line ~65), so the CLI path is safe. **Verify the existing try/except actually swallows the relevant exception type** during the CLI test.\n\n### MODIFY: `serverless-docker/.circleci/config.yml` (setup config)\nAdd a separate filter so tags matching `installer-v.*` route to a different continuation config (or extend `generate_continue.py` to emit a different workflow when CIRCLE_TAG starts with `installer-`).\n\n**Recommended approach:** keep ONE continuation config; branch inside it by tag pattern. The setup job emits BOTH `build_worker` and `build_installer` workflows, each gated on its respective tag prefix.\n\n### MODIFY: `serverless-docker/.circleci/generate_continue.py`\nAdd a second workflow alongside `build-deploy-smoke`:\n```yaml\nworkflows:\n  build-installer:\n    jobs:\n      - build_and_push_installer:\n          context: [docker-hub, flowbot-webhook-token]\n          filters:\n            tags:\n              only: /^installer-v.*$/\n            branches:\n              ignore: /.*/\n```\n\nThe `build_and_push_installer` job mirrors `build_and_push` but:\n- Uses `-f Dockerfile.installer` in the `docker buildx build` invocation\n- Tags as `hearmeman/comfyui-serverless:${CIRCLE_TAG}` (e.g. `installer-v1`)\n- Cache target: `hearmeman/comfyui-serverless:installer-buildcache` (separate from worker buildcache)\n- No update_endpoint / wait_for_rollout / smoke_test — image is tested by live install runs from BlockFlow\n\nThe existing `build-deploy-smoke` workflow keeps its `^v.*$` filter (NOT `^installer-v.*$`) so `installer-v1` doesn't trigger the worker pipeline. **Verify the existing regex `/^v.*$/` doesn't match `installer-v1` — it doesn't, since the regex is anchored at start with `v` not `installer-v`. Confirm in the generated config.**\n\n### MODIFY: `tests/test_download_handler.py`\nAdd cases for the CLI entrypoint (next section).\n\n---\n\n## TDD plan — task-by-task\n\n### Task 1: download_handler CLI entrypoint\n**Files:** modify `serverless-runtime/download_handler.py`, add tests to `tests/test_download_handler.py`\n\n**Steps:**\n- [ ] Write failing tests for `_cli_main`:\n  1. happy path: job dict from stdin → handle() runs → JSON to stdout → exit 0\n  2. job from --job FILE: same, but reads from a file path\n  3. handle() returns ok=false → exit 1\n  4. malformed JSON in stdin → JSONDecodeError raises (don't catch; let pytest see it)\n  5. _send_progress noop when runpod harness absent — runs to completion without error\n- [ ] Run pytest, expect failures (function doesn't exist)\n- [ ] Implement `_cli_main` + `if __name__ == \"__main__\"` block\n- [ ] Verify _send_progress already has its try/except — read `runpod.serverless.progress_update` call and confirm the exception path is caught even when runpod is uninstalled (it's in a try/except Exception — broad enough)\n- [ ] Run pytest, all pass\n- [ ] Commit: \"Add download_handler CLI entrypoint for CPU installer pod\"\n- [ ] Push to remote-comfy-gen-handler\n\n### Task 2: install-entrypoint.sh\n**Files:** create `serverless-docker/install-entrypoint.sh`\n\n**Steps:**\n- [ ] Write the script per the spec above\n- [ ] Local smoke (without Docker): set PRESET_ID=qwen-image-lighting + RUNTIME_REPO_URL + use a fake `/runpod-volume` via `mkdir /tmp/fake-volume \u0026\u0026 PRESET_ID=qwen-image-lighting bash install-entrypoint.sh` (won't download, will fail at git clone or at /runpod-volume not mounted check — which is good; we're testing the script's branching, not the downloads)\n- [ ] Test missing PRESET_ID → script exits 1 with \"PRESET_ID env var required\"\n- [ ] Test missing /runpod-volume → script exits 2 with \"FATAL: /runpod-volume not mounted\"\n- [ ] Test PRESET_ID not in manifest → curl/python step fails with StopIteration; script exits via set -e\n- [ ] Commit: \"Add CPU installer pod entrypoint\"\n\n### Task 3: Dockerfile.installer + CI workflow\n**Files:** create `serverless-docker/Dockerfile.installer`, modify `serverless-docker/.circleci/generate_continue.py`\n\n**Steps:**\n- [ ] Write Dockerfile.installer per spec\n- [ ] Modify generate_continue.py to emit BOTH workflows; add tag-pattern branching\n- [ ] `circleci config validate` on the generated continuation YAML — must pass\n- [ ] Local docker build test (skip if no docker locally): `docker buildx build -f Dockerfile.installer -t test-installer .` — expect ~200 MB image, exits clean\n- [ ] Commit + push to serverless-docker\n- [ ] Tag `installer-v1` + push tag\n\n### Task 4: Live verification (manual)\n**Steps:**\n- [ ] Wait for CircleCI `build-installer` workflow to complete → `hearmeman/comfyui-serverless:installer-v1` lives on Docker Hub\n- [ ] Manually spawn a CPU pod via RunPod GraphQL (one-shot, with the image + qwen-image-lighting preset). Recipe to record in bead notes:\n  ```graphql\n  mutation { podFindAndDeployOnDemand(input: {\n    cloudType: COMMUNITY, gpuCount: 0, minVcpuCount: 2, minMemoryInGb: 4,\n    networkVolumeId: \"7etzak7vfp\",\n    containerDiskInGb: 10,\n    imageName: \"hearmeman/comfyui-serverless:installer-v1\",\n    env: [\n      {key: \"PRESET_ID\", value: \"qwen-image-lighting\"},\n      {key: \"CIVITAI_TOKEN\", value: \"...\"},\n    ]\n  }) { id imageName } }\n  ```\n- [ ] Poll the pod's logs via RunPod /pods/\u003cid\u003e/logs every 10s until pod exits\n- [ ] Verify exit code 0 (or read it from pod status)\n- [ ] Verify all 4 qwen models are on the volume by submitting a fresh `comfy-gen list diffusion_models --endpoint-id 5fkobbksgrtww6` (or by spawning a brief check pod that ls's `/runpod-volume/ComfyUI/models/`)\n- [ ] Record total elapsed time + cost — should be ~5 min and ~$0.05 (the qwen preset is ~52GB cached, but if volume already has them, 0 downloads → ~30s)\n- [ ] For a real cost demo: pick a NOT-already-installed preset (or fresh volume), run install, time it\n- [ ] Close bead with the numbers recorded in notes\n\n---\n\n## Done criteria (verification target)\n\n- 5 unit tests for `_cli_main` pass (stdin path, file path, exit-1 path, edge cases)\n- Dockerfile.installer builds cleanly, image is \u003c 300 MB\n- CircleCI `build-installer` workflow runs green on `installer-v1` tag\n- Live: spawning the pod with PRESET_ID=qwen-image-lighting downloads all 4 models to /runpod-volume, pod exits 0, total wall time + cost recorded in bead notes\n- A second tag (`installer-v2`) builds in \u003c 1 min via cache (proves cache works)\n\n---\n\n## Edge cases to handle (per project TDD doctrine — non-negotiable)\n\n| Edge case | Expected behavior |\n|---|---|\n| PRESET_ID not in manifest | entrypoint exits non-zero with clear error |\n| /runpod-volume not mounted | exits 2 with \"volume not mounted\" |\n| /runpod-volume not writable | exits 2 with \"volume not writable\" |\n| CIVITAI_TOKEN required by a preset model but absent | handler raises clean error; pod exits non-zero |\n| Network failure mid-download | aria2c retries (built-in); on final fail handle() returns ok=false, exits 1 |\n| sha256 mismatch on downloaded file | handle() removes the corrupt file, returns ok=false, exits 1 |\n| Preset has 0 models (workflow-only) | handle() returns ok=true, exits 0 (no-op) |\n| Volume has the model already with matching sha256 | dedup hits; aria2c not invoked; result shows `cached: true` |\n| Preset.json malformed | curl succeeds; python translation fails; pod exits non-zero |\n| RUNTIME_REPO_REF doesn't exist (typo) | git clone fails; pod exits non-zero |\n\n---\n\n## Cross-references\n\n- **Blocks:** sgs-ui-ngz (BlockFlow installer needs this image to exist on Docker Hub before it can spawn it)\n- **Related code:** existing `serverless-docker/start_script.sh` (the GPU image's runtime clone pattern — mirror it in install-entrypoint.sh)\n- **Existing tests to NOT break:** all of `tests/test_download_handler.py` (the new __main__ entrypoint must not change worker-dispatch behavior)","notes":"SHIPPED + LIVE-VERIFIED 2026-05-24.\n\nFinal image: hearmeman/comfyui-serverless:installer-v4 (Docker Hub).\n\nLive verification (cached install — qwen-image-lighting models already on volume 7etzak7vfp):\n  Pod e4a27w2d10c3yd · cpu3c · 4 vCPU / 8 GB · US-NC-2\n  Wall time: 56s (spawn -\u003e self-terminate)\n  Cost: $0.0019 at $0.12/hr\n  Handler output: {\"ok\": true, \"files\": [\u003c4 entries\u003e]}\n  All 4 models sha256-verified.\n  vs prior GPU-worker install cost: ~$1.50 → ~800x cost reduction on cached path.\n\nTags: installer-v1 (empty, superseded), v2 (mount bug), v3 (worked but restart-looped), v4 (final).\n\nTwo corrections to original bead spec discovered during live verification:\n1. RunPod pods mount network volumes at /workspace, not /runpod-volume. Handled by\n   entrypoint-side symlink so download_handler.py's hardcoded MODELS_BASE stays correct.\n2. Pods bill for full wall time the pod is allocated — CMD exiting does NOT stop billing\n   (only true for serverless workers). Entrypoint now self-DELETEs via REST when\n   RUNPOD_API_KEY is in env. BlockFlow's spawn call must include that env var.","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-24T04:57:59Z","created_by":"Hearmeman24","updated_at":"2026-05-24T05:35:14Z","started_at":"2026-05-24T05:06:42Z","closed_at":"2026-05-24T05:35:14Z","close_reason":"Closed","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-kv9","title":"[smoke gate] Handle multi-workflow presets + smoke_inputs (wan-animate)","description":"Today smoke_preset.py:106 hardcodes preset[\"workflow\"][\"url\"] (singular). Crashes immediately on wan-animate, which uses preset[\"workflows\"][] (array, 3 entries). Also doesn't pass file_inputs to comfy-gen submit, so workflows that need image/video files would fail at LoadImage/VHS_LoadVideo even if it got past the schema crash.\n\n**Locked design (from session 2026-05-23):**\n- Multi-workflow handling: run only the FIRST listed workflow per preset (cheapest; partial coverage). For wan-animate that's 'Keep Background', which matches preset.tested_against.note.\n- Input files: preset declares per-workflow smoke_inputs (schema work in sgs-ui-5ir)\n- Cost: full matrix every release (wan-animate runs every tag-push)\n\n**Smoke schema handling (in smoke_preset.py):**\n1. `workflow_entry = preset.get('workflow') or preset['workflows'][0]` — support both shapes\n2. If `workflow_entry.smoke_inputs` present: fetch each fixture (with sha256 verify), build file_inputs dict keyed by node_id, pass via `comfy-gen submit --file-inputs \u003cjson\u003e` (CLI may need this flag added)\n3. If smoke_inputs absent on a workflow that needs files: smoke should FAIL with a clear error (\"workflow has LoadImage/VHS_LoadVideo but no smoke_inputs declared\") rather than crash deep in ComfyUI runtime\n\n**Files to touch:**\n- automation/smoke_preset.py — schema handling + file_inputs plumbing\n- comfy_gen/cli.py / comfy_gen/serverless.py — verify --file-inputs is exposed at CLI level; if not, add it (currently only the Python API takes file_inputs)\n- tests/test_smoke_preset.py — new test file for the multi-workflow / smoke_inputs cases (pure unit tests mocking the file fetches)\n\n**Acceptance:**\n- wan-animate preset's 'Keep Background' workflow runs cleanly in v23 smoke matrix (with sgs-ui-5ir landed)\n- qwen-image-lighting still works (regression: legacy singular schema)\n- Unit tests cover: legacy preset.workflow shape, new preset.workflows[0] shape, smoke_inputs fetched + passed to submit, sha256 mismatch errors clearly, missing smoke_inputs on file-requiring workflow surfaces a useful error\n\n**Blocked by:** sgs-ui-5ir (schema field + wan-animate preset update + fixtures committed)","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T20:19:37Z","created_by":"Hearmeman24","updated_at":"2026-05-23T20:36:18Z","started_at":"2026-05-23T20:34:15Z","closed_at":"2026-05-23T20:36:18Z","close_reason":"Closed","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-dio","title":"[perf] Registry-backed BuildKit cache + cache mounts + layer reorder","description":"Take the highest-impact items from the audit: ~13 min build → ~3 min on no-op, ~6 min on typical 1-node bumps.\n\n**Items in scope (audit's PR1):**\n1. Switch `docker build` → `docker buildx build --cache-to=type=registry,ref=docker.io/hearmeman/comfyui-serverless:buildcache,mode=max --cache-from=type=registry,ref=docker.io/hearmeman/comfyui-serverless:buildcache --push` in generate_continue.py\n2. Add `# syntax=docker/dockerfile:1.4` to Dockerfile + `RUN --mount=type=cache,target=/root/.cache/pip,sharing=locked` on pip steps (ComfyUI core requirements, sageattention, runpod/boto3, the 28-node loop)\n3. Reorder layers: put least-changing COPY operations first (start_script.sh, log_forwarder.py, extra_model_paths.yaml after the verify import — currently they invalidate verify)\n4. Polish: `--depth 1` on ComfyUI core + node clones, `--filter=blob:none` fallback, `--no-cache-dir` on pip calls\n\n**Out of scope (separate beads if needed):**\n- Per-node RUN layers (audit item #4 — moderate complexity)\n- Multi-stage build to cudnn-runtime base (audit item #5 — careful smoke needed)\n\n**Acceptance:**\n- v21 first-build populates the registry cache successfully\n- v22 no-op rebuild (same lockfile) finishes \u003c5 min\n- Smoke matrix still green\n- Dockerfile validated via `docker buildx build --dry-run` locally OR by the actual v21 build\n\n**Reference:** see audit report from session 2026-05-23 — registry cache is the biggest single win; DLC explicitly NOT worth it.","notes":"v21 first-build measurements:\n- build_and_push: 9.5 min (down from ~13 min)\n- image size: 11.3 GB (down from 16 GB, -29%)\n- buildcache tag populated on Docker Hub (11.3 GB)\n- smoke_test still green\n\nExpected v22 numbers (next no-op build):\n- build_and_push: ~3-4 min (cache hit on every layer)\n- typical 1-node SHA bump: ~6 min (apt/comfy/sage/runpod cached, only node-loop re-runs)\n\nImage-size win (-4.7 GB) was an unintended bonus from --filter=blob:none + apt cache mounts. Will speed up every RunPod cold-start image pull by ~30%.","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T18:53:33Z","created_by":"Hearmeman24","updated_at":"2026-05-23T19:23:25Z","started_at":"2026-05-23T18:53:43Z","closed_at":"2026-05-23T19:23:25Z","close_reason":"Closed","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-6qq","title":"[docs] Document the release flow (freeze → tag → push) in CLAUDE.md","description":"**Scope:** add a 'Releasing a new docker image' section to serverless-docker/CLAUDE.md (and a pointer from the outer CLAUDE.md) describing:\n\n1. Author/test workflows locally.\n2. Run 'freeze' to snapshot SHAs from local.\n3. Review the nodes.lock diff (will show drift since last freeze).\n4. Commit + tag + push.\n\nAlso document the list-mismatch warnings the freeze script emits and how to act on them (add to Dockerfile, or accept as known docker-only).","status":"closed","priority":2,"issue_type":"task","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T16:47:17Z","created_by":"Hearmeman24","updated_at":"2026-05-23T18:08:42Z","started_at":"2026-05-23T18:07:30Z","closed_at":"2026-05-23T18:08:42Z","close_reason":"Closed","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-q88","title":"[hotpatch] kijai/ComfyUI-WanAnimatePreprocess ONNX dropdown bug at startup","description":"**Scope:** add hotpatch to serverless-runtime/start.sh that runs before ComfyUI launches:\n\nDetects /ComfyUI/custom_nodes/ComfyUI-WanAnimatePreprocess/nodes.py, checks if already patched (grep filename_list_cache), and if not, injects the fix from upstream issue #32:\n\n  if 'detection' in folder_paths.folder_names_and_paths:\n      paths, exts = folder_paths.folder_names_and_paths['detection']\n      folder_paths.folder_names_and_paths['detection'] = (paths, set(exts) | {'.onnx'})\n      if hasattr(folder_paths, 'filename_list_cache') and 'detection' in folder_paths.filename_list_cache:\n          del folder_paths.filename_list_cache['detection']\n      if hasattr(folder_paths, 'cache_helper'):\n          folder_paths.cache_helper.clear()\n\n**Acceptance:** OnnxDetectionModelLoader dropdown shows .onnx files after a cold start with the patch applied. Hotpatch is idempotent (re-running on already-patched file is a no-op).\n\n**Note:** can be dropped once the pinned-SHA approach + an upstream PR (separate bead) get the fix merged. Until then this is the ship-today fix.","status":"closed","priority":2,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T16:47:16Z","created_by":"Hearmeman24","updated_at":"2026-05-23T17:57:58Z","started_at":"2026-05-23T17:53:31Z","closed_at":"2026-05-23T17:57:58Z","close_reason":"Closed","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-k5u","title":"[smoke gate] Detect when workflow class_types aren't installed in the image","description":"# k5u — smoke-gate workflow pre-validation\n\n\u003e **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox syntax.\n\n**Goal:** Catch \"class_type not installed\" / \"Required input is missing\" / \"Value not in list\" failures BEFORE the worker tries to execute, by pre-validating each workflow against ComfyUI's /object_info inside the smoke matrix.\n\n**Architecture:** New `object_info` worker command exposes ComfyUI's INPUT_TYPES; a pure validator in automation/ compares a workflow JSON against that spec; smoke_preset.py calls both between download and submit, fails fast with all problems named. Also adds `comfy-gen object-info` CLI for ad-hoc debugging.\n\n**Tech Stack:** stdlib only (urllib + json), pytest, pytest-mock.\n\n---\n\n## File structure\n\n- CREATE `serverless-runtime/object_info_handler.py` — worker handler (uses info_handler._get_object_info)\n- MODIFY `serverless-runtime/worker.py` — add object_info to dispatch block (after volume_info entry)\n- MODIFY `tests/conftest.py` — add object_info branch to dispatch_command fixture\n- CREATE `tests/test_object_info_handler.py` — 4 cases\n- CREATE `automation/validate_workflow.py` — pure validate(workflow, object_info) -\u003e list[str]\n- CREATE `tests/test_validate_workflow.py` — 6 cases\n- MODIFY `.gitignore` — add `!automation/validate_workflow.py` and `!automation/smoke_preset.py` (latter already tracked — double-check)\n- CREATE `comfy_gen/object_info.py` — submit/poll shim (mirror of hash_files.py)\n- MODIFY `comfy_gen/cli.py` — add cmd_object_info + parser + dispatcher entry\n- MODIFY `automation/smoke_preset.py` — call object_info worker + validate() between download and submit\n\n---\n\n## Dependency DAG\n\n```\nTask 1 (worker handler) ───┐\n                            ├──→ Task 3 (CLI) ──┐\nTask 2 (validator) ─────────┴───────────────────┴──→ Task 4 (smoke integration + live verify)\n```\n\nTasks 1 and 2 are independent — parallelizable.\n\n---\n\n### Task 1: Worker `object_info` command\n\n**Files:**\n- Create: serverless-runtime/object_info_handler.py\n- Modify: serverless-runtime/worker.py (add dispatch entry alongside the existing hash entry)\n- Modify: tests/conftest.py (add object_info branch to dispatch_command fixture)\n- Create: tests/test_object_info_handler.py\n\n**Depends on:** none\n\n**Steps:**\n- [ ] Write failing handler tests covering:\n  1. no filter → returns all classes\n  2. filter=[\"KSampler\"] → returns ONLY KSampler (and ok=true)\n  3. filter=[\"NonexistentClass\"] → returns {} for classes (ok=true, batch ran fine)\n  4. upstream ComfyUI HTTP error → returns {ok: false, error: \u003cmsg\u003e}\n  Mock urllib.request.urlopen for /object_info; assert handler shape.\n- [ ] Run pytest tests/test_object_info_handler.py — expect ImportError or ValueError (handler not wired)\n- [ ] Implement object_info_handler.py: handle(job) reads job_input.get(\"class_types\"), calls info_handler._get_object_info() (reuse), filters dict, returns {\"ok\": true, \"classes\": ...} or {\"ok\": false, \"error\"} on URLError. Wire into worker.py + conftest fixture.\n- [ ] Run pytest — expect all 4 pass\n- [ ] Commit (runtime repo): \"Add object_info worker command for workflow pre-flight\"\n\n### Task 2: Pure validator `automation/validate_workflow.py`\n\n**Files:**\n- Create: automation/validate_workflow.py\n- Create: tests/test_validate_workflow.py\n- Modify: .gitignore (un-ignore validate_workflow.py)\n\n**Depends on:** none\n\n**Steps:**\n- [ ] Update .gitignore: add `!automation/validate_workflow.py` near existing `!automation/smoke_preset.py` rule\n- [ ] Write failing validator tests covering:\n  1. clean workflow (class exists, required inputs provided, enum values valid) → returns []\n  2. missing class → returns [\"Node 5 (NonexistentNode): class not installed\"]\n  3. missing required input → returns [\"Node 7 (KSampler): missing required input 'seed'\"]\n  4. bad enum value → returns [\"Node 9 (Loader): 'foo.onnx' not in ['bar.bin']\"]\n  5. connection-typed input [node_id, idx] → SKIPPED from both enum and required checks (input is provided as a connection, satisfies \"provided\"; enum check doesn't apply)\n  6. multiple problems → all reported in one list\n  Construct synthetic workflow + object_info dicts inline; no I/O.\n- [ ] Run pytest tests/test_validate_workflow.py — expect ImportError\n- [ ] Implement validate(workflow, object_info) -\u003e list[str]. Iterate workflow.items(): per node, check class_type in object_info; per literal input, enum-check; per required input declared in object_info, presence-check (literal OR connection counts as provided).\n- [ ] Run pytest — expect all 6 pass\n- [ ] Commit (outer repo): \"Add pure workflow validator for smoke pre-flight\"\n\n### Task 3: CLI subcommand `comfy-gen object-info`\n\n**Files:**\n- Create: comfy_gen/object_info.py (submit + poll wrapper, mirror hash_files.py)\n- Modify: comfy_gen/cli.py (cmd_object_info + parser + dispatcher)\n\n**Depends on:** Task 1\n\n**Steps:**\n- [ ] Create comfy_gen/object_info.py: submit_object_info(class_types, ...) → calls /v2/{ep}/run with {input: {command: object_info, class_types: [...]}}, poller.poll_job, returns result. Pattern-copy from comfy_gen/hash_files.py.\n- [ ] In cli.py: cmd_object_info reads positional class_types (nargs='*'); add p_object_info parser with verbose --help (mirror p_hash); register \"object-info\": cmd_object_info in the dispatcher dict.\n- [ ] Run `comfy-gen object-info --help` — verify help renders, no parse errors\n- [ ] Live test: `comfy-gen object-info KSampler --endpoint-id 5fkobbksgrtww6` — expect JSON with KSampler INPUT_TYPES (sampler enum, etc.)\n- [ ] Commit (outer repo): \"Add comfy-gen object-info CLI subcommand\"\n\n### Task 4: Smoke gate integration + live verification\n\n**Files:**\n- Modify: automation/smoke_preset.py (add validation step between download and submit)\n\n**Depends on:** Tasks 1, 2, 3\n\n**Steps:**\n- [ ] In smoke_preset.py, after the download step + before fetching the workflow for submit:\n  1. Parse the local workflow JSON for distinct class_types\n  2. Call run_cli([\"comfy-gen\", \"object-info\", *class_types, \"--endpoint-id\", ep]) → JSON result\n  3. Import validate_workflow.validate, call validate(workflow, result[\"classes\"])\n  4. If returned list non-empty: raise RuntimeError with all failures joined (\"\\n  - \")\n- [ ] Live test against qwen-image-lighting endpoint 5fkobbksgrtww6: full smoke run should still pass green (workflow is valid)\n- [ ] Push runtime + outer repos\n- [ ] Tag v22 in serverless-docker (no Dockerfile changes — just triggers full pipeline to validate end-to-end via the existing smoke matrix)\n- [ ] Watch v22 pipeline → expect build-deploy-smoke = success (smoke now exercises the new validator on the live preset)\n- [ ] Close bead k5u\n\n---\n\n## Done criteria (verification target)\n\n- All 4 handler tests pass\n- All 6 validator tests pass\n- `comfy-gen object-info KSampler --endpoint-id ...` returns sane JSON\n- v22 smoke matrix passes green on qwen-image-lighting (validator does NOT false-positive a real working preset)\n- Bonus (optional): construct a deliberately-broken workflow JSON, point validator at it via `python3 -c \"from automation.validate_workflow import validate; ...\"`, confirm it reports the expected failures","design":"Workflow pre-flight validation against ComfyUI's /object_info. Three components:\n\n## Decision tree (locked via grill 2026-05-23)\n\n| Q | Decision | Why |\n|---|---|---|\n| Worker command shape | NEW `object_info` (not extend query_info) | query_info has different scope (samplers/schedulers/loras); conflating bloats payload + entangles concerns |\n| Validation depth | Minimal: class exists + required inputs present + enum values valid | Catches the 3 failure modes actually hit in v18/v19/v20. Type/range checking has false-positive risk (e.g., runtime-resolved values from upstream connections) |\n| Wiring scope | smoke_preset.py only (NOT comfy-gen submit) | Matches bead scope; everyday submit stays fast. Submit-side gate can be a follow-up bead if desired later |\n| CLI surface | Add `comfy-gen object-info \u003cClass1\u003e \u003cClass2\u003e` | Small (~30 lines, mirrors `comfy-gen hash`), useful for ad-hoc diagnosis of future \"Value not in list\" issues |\n\n## Architecture\n\n**1. Worker command (serverless-runtime/object_info_handler.py)**\n\nAccepts: `{command: \"object_info\", class_types?: [\"Class1\", \"Class2\", ...]}` (filter optional, omit = all)\nReturns: `{ok: true, classes: {\u003cClassName\u003e: {input: {required, optional}, output, output_name, ...}}}`\n\nReuses `info_handler._get_object_info()` to fetch /object_info; filters the dict if `class_types` provided. On upstream HTTP error returns `{ok: false, error: \u003cmsg\u003e}`.\n\nWired into:\n- `serverless-runtime/worker.py` dispatch block\n- `tests/conftest.py` dispatch_command fixture\n\n**2. Pure validator (automation/validate_workflow.py)**\n\n```python\ndef validate(workflow: dict, object_info: dict) -\u003e list[str]:\n    \"\"\"Return list of human-readable failure strings (empty = workflow is valid).\"\"\"\n```\n\nPer workflow node:\n- If class_type missing from object_info → \"Node \u003cid\u003e (\u003cct\u003e): class not installed\"\n- For each literal input value (skip lists shaped [node_id, output_idx] — those are connections):\n  - If field's spec[0] is a list-of-strings (enum) and value not in it → \"Node \u003cid\u003e (\u003cct\u003e): '\u003cvalue\u003e' not in \u003cenum\u003e\"\n- For each declared REQUIRED input in object_info that workflow doesn't provide → \"Node \u003cid\u003e (\u003cct\u003e): missing required input '\u003cname\u003e'\"\n\nPure function, no I/O. Trivially unit-testable.\n\n**3. smoke_preset.py integration**\n\nBetween `comfy-gen download` and `comfy-gen submit`:\n1. Extract distinct class_types from workflow JSON\n2. Call `comfy-gen object-info --class-types \u003cct1\u003e,\u003cct2\u003e,...` (or call worker directly)\n3. Run `validate(workflow, object_info)`; if failures → raise with the full list\n4. Proceed to submit\n\nSmoke job fails fast with all problems named in one error, instead of waiting ~25 min for the worker to hit the same validation and return a cryptic 1-of-N message.\n\n**4. CLI (comfy_gen/cli.py + comfy_gen/object_info.py)**\n\n`comfy-gen object-info \u003cClass1\u003e \u003cClass2\u003e [--endpoint-id ID]` — thin wrapper over the worker command, JSON to stdout. Mirrors the `comfy-gen hash` pattern exactly.\n\n## Done criteria (verification target)\n\n- tests/test_object_info_handler.py: 4 cases — empty filter (returns all), specific filter (returns subset), unknown class (returns empty key, ok=true), upstream ComfyUI 500 (returns {ok: false, error})\n- tests/test_validate_workflow.py: 6 cases — clean workflow passes; missing class detected; missing-required-input detected; bad-enum-value detected; connection-input ([node_id, idx]) skipped; multiple failures all reported\n- Live: smoke run on qwen-image-lighting stays green (no false positives)\n- Live: validator run against user's Wan_Animate.json should report the real bugs (OpenRouterNode.image_resolution and OnnxDetectionModelLoader.yolo_model)","notes":"Live validation on v22 pipeline #35:\n\n| Job | Duration | Result |\n|---|---|---|\n| build_and_push | 0.2m | full cache hit (registry-backed buildx cache from v21 paid out) |\n| update_endpoint | 0.1m | |\n| wait_for_rollout | 0.3m | workers already on v22 from prior attempt |\n| smoke_test (qwen-image-lighting) | 1.2m | download + object_info pre-flight + submit + verify URL |\n| notify_done | 0.1m | |\n\n**Total: 1.9 min wall time** for the full release. Smoke gate's new k5u pre-flight successfully ran without false-positive on the real qwen-image-lighting workflow.\n\nImplementation:\n- 4 worker handler tests + 6 validator tests (10 new unit tests, all passing in pytest)\n- serverless-runtime/object_info_handler.py + worker.py dispatch + conftest fixture\n- automation/validate_workflow.py (pure function)\n- comfy_gen/object_info.py + comfy_gen/cli.py (CLI subcommand `comfy-gen object-info`)\n- automation/smoke_preset.py wired the validator between download and submit\n- CI fix: also fetch validate_workflow.py alongside smoke_preset.py in the CI continuation config\n\nCommits:\n- runtime: 18f6e69 (object_info worker)\n- outer: e226d7d (tests), 2dea22e (validator), 886e7c8 (CLI), 79992f3 (smoke wiring)\n- docker: 2f734f1 (CI fetch fix)","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T16:47:15Z","created_by":"Hearmeman24","updated_at":"2026-05-23T20:02:10Z","started_at":"2026-05-23T19:41:39Z","closed_at":"2026-05-23T20:02:10Z","close_reason":"Closed","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-pu2","title":"Include .onnx (and .gguf) in list_handler MODEL_EXTENSIONS","description":"serverless-runtime/list_handler.py:20 filters scanned files by MODEL_EXTENSIONS = {.safetensors, .ckpt, .pt, .pth, .bin}. ONNX models (vitpose, yolo, sam, etc.) and GGUF quantized models are invisible to 'comfy-gen list \u003ctype\u003e' even when on disk. Live-confirmed against detection/ on 5fkobbksgrtww6: 3 files on volume, only the .bin returns.\n\nFix: add .onnx and .gguf to the allowlist. Update test to assert .onnx file appears.","status":"closed","priority":2,"issue_type":"bug","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T14:33:57Z","created_by":"Hearmeman24","updated_at":"2026-05-23T14:36:57Z","started_at":"2026-05-23T14:34:18Z","closed_at":"2026-05-23T14:36:57Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-9ws","title":"CircleCI post-build: rolling release + preset smoke gate","description":"Extend serverless-docker CircleCI workflow with three new jobs after build_and_push:\n\n1. **update_endpoint** — PATCH RunPod template imageName to the new tag (CIRCLE_TAG), triggers rolling release on endpoint 5fkobbksgrtww6\n2. **wait_for_rollout** — poll GET /endpoints/{id}?includeWorkers=true until all workers report image ending in :$CIRCLE_TAG and at least workersMin are READY. Timeout ~30 min.\n3. **smoke_test** — matrix per preset in Hearmeman24/blockflow-presets manifest. For each preset:\n   - Fetch preset.json\n   - Reimplement the install flow inline: build downloads.json from preset's models (with sha256), 'comfy-gen download --batch', 'comfy-gen submit' the workflow, GET-Range verify each output URL\n   - Pass = all outputs verified within preset.execution_timeout\n4. **notify** — FlowBot post (already exists, may need extending to summarize matrix results)\n\nEach job uses 'requires:' so rerun-from-failed re-runs only the failed job — no Docker rebuild on smoke failure (image is already on Docker Hub from build_and_push).\n\n**Decisions locked** (from session 2026-05-22):\n- Target endpoint: prod (5fkobbksgrtww6), no ephemeral\n- Smoke scope: install + workflow-run + output-URL verify, every preset\n- On smoke failure: fail loud, no auto-revert (FlowBot ping + CircleCI red)\n- Parallelism: CircleCI matrix per preset (CircleCI Dynamic Config needed since preset list is fetched at runtime from manifest.json)\n\n**New project env vars needed in CircleCI:**\n- RUNPOD_API_KEY (for /endpoints/, /templates/ calls)\n- CIVITAI_TOKEN (if any future preset uses civitai source — current sole preset 'qwen-image-lighting' is HF-only)\n- Maybe HF token for gated HF model URLs\n\n**Cost estimate:** at current 1-preset registry, ~25 min wall + ~$1 RunPod per release. Scales linearly with preset count (matrix parallelism keeps wall time = slowest preset).","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-22T09:40:14Z","created_by":"Hearmeman24","updated_at":"2026-05-22T12:01:14Z","started_at":"2026-05-22T09:45:06Z","closed_at":"2026-05-22T12:01:14Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-4ci","title":"Add comfy-gen delete CLI subcommand (wraps existing delete worker command)","description":"The `delete` worker command (bead f9d) was added in this session but isn't exposed in the CLI yet. Add a thin CLI shim mirroring `comfy-gen hash`:\n\n- `comfy-gen delete \u003cpath\u003e...` (positional) and `--batch paths.json` (array of strings)\n- POST to the worker as `{command: 'delete', paths: [...]}`, poll, print JSON result to stdout\n- Verbose --help per project convention; document the realpath-based security constraint\n- Register in the cli dispatcher\n\nNo worker work needed — `delete_handler.py` already exists and is tested.","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-21T15:54:53Z","created_by":"Hearmeman24","updated_at":"2026-05-21T15:56:38Z","started_at":"2026-05-21T15:54:59Z","closed_at":"2026-05-21T15:56:38Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-ykr","title":"Add hash worker command + CLI for sha256 of existing files (enables client-side dedup)","description":"**Context:** Without an on-demand way to hash files already on the network volume, callers can't decide whether to skip a download. The new download_handler dedup (h3o) needs the caller to supply sha256 — so the caller needs a way to ask 'what's the sha256 of this file you already have?'\n\n**What's needed:**\n\n1. **Worker command** `hash` in `serverless-runtime/hash_handler.py` — accepts `{command: \"hash\", paths: [...]}`, returns `{ok, files: [{path, sha256, bytes, error?}]}`. Security: same as delete_handler — every path realpath-resolved and must land under /runpod-volume.\n\n2. **CLI subcommand** `comfy-gen hash \u003cpath\u003e...` (and `--batch \u003cfile.json\u003e` like download). JSON output: `{ok, files: [{path, sha256, bytes, error?}]}`.\n\n3. Wire `hash` into `worker.py` dispatch.\n\n4. Mirror dispatch in `tests/conftest.py` `dispatch_command` fixture.\n\n5. Document in CLAUDE.md Worker Job Commands list + README.\n\n**Acceptance:**\n- `command: \"hash\"` accepts list of paths, returns per-path sha256 + bytes\n- Paths outside /runpod-volume rejected (path traversal + symlink escape resolved via realpath)\n- Missing file → per-path error, not job failure (idempotent batch)\n- Directory path → per-path error\n- Streamed hashing (large model files don't blow memory)\n- CLI emits valid JSON to stdout, human logs to stderr\n- Verbose --help per project convention","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-21T15:42:50Z","created_by":"Hearmeman24","updated_at":"2026-05-21T15:46:29Z","started_at":"2026-05-21T15:42:56Z","closed_at":"2026-05-21T15:46:29Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-ema","title":"Add volume_info worker command for disk-space queries (BlockFlow OSS push)","description":"**Context:** BlockFlow's preset installer (`sgs-ui-wisp-las.3`) needs to check ComfyGen's network volume free space before starting a preset install. Each preset's manifest declares a `disk_size_estimate_gb`; BlockFlow blocks install if `estimate * 1.2 \u003e available` and tells the user to uninstall presets in Settings \u003e Storage to free space.\n\n**Current state (audit):** No volume info / disk space query exists. `serverless-runtime/storage.py` handles S3 uploads but doesn't touch the local filesystem for disk metrics. No `df` shell call or `os.statvfs()` anywhere in the runtime.\n\n**Without this:** preset installs run to completion before the user discovers they're out of disk. ComfyGen errors with a generic \"no space left\" mid-download; partial files litter the volume; BlockFlow's resume-on-partial-fail flow has to clean up afterward. Bad UX, wasted bandwidth.\n\n**What's needed:**\n- New file `serverless-runtime/volume_info_handler.py` exposing a `command == \"volume_info\"` handler.\n- Returns `{ok: true, path: \"/runpod-volume\", size_bytes: \u003cint\u003e, used_bytes: \u003cint\u003e, free_bytes: \u003cint\u003e}` via `os.statvfs(\"/runpod-volume\")`.\n- Update `worker.py` (~lines 617-626) to dispatch the new command.\n\n**Acceptance:**\n- [ ] `command: \"volume_info\"` job returns total/used/free bytes for `/runpod-volume`\n- [ ] Returns fast (just a syscall; no GPU, no model loading)\n- [ ] Documented in ComfyGen docs as part of the worker job command set\n\n**Effort estimate (from audit):** ~1 hour, ~30 LOC\n\n**Cross-project reference:** BlockFlow `sgs-ui-wisp-las.3` (Preset installer). Acceptance criterion: \"Disk-space pre-check: query ComfyGen volume free space; block install if `disk_size_estimate * 1.2` \u003e available; show clear 'need X GB, have Y GB' with link to Settings \u003e Storage.\" Will also surface as a \"Storage\" UI in BlockFlow Settings showing volume usage.","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-21T07:48:36Z","created_by":"Hearmeman24","updated_at":"2026-05-21T08:37:50Z","started_at":"2026-05-21T08:35:25Z","closed_at":"2026-05-21T08:37:50Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-f9d","title":"Add delete worker command for path-based file removal (BlockFlow OSS push)","description":"**Context:** BlockFlow's preset installer (`sgs-ui-wisp-las.3`) needs to delete model files from ComfyGen's network volume to support preset uninstall. Without this, disk fills with orphaned models from uninstalled presets, defeating the marketplace UX.\n\n**Current state (audit):** No file-deletion command exists. `worker.py` dispatches only `download`, `list_models`, `query_info`. `download_handler.py` adds files; nothing removes them.\n\n**Without this:** BlockFlow's \"Uninstall\" button in the preset UI is non-functional, or has to delegate to manual user action via the RunPod console. v1 acceptance criteria for `sgs-ui-wisp-las.3` (\"Uninstall: confirm → ComfyGen delete-files command for each path → Settings updated → preset hidden from comfy_gen dropdown\") cannot be met without this capability.\n\n**What's needed:**\n- New file `serverless-runtime/delete_handler.py` exposing a `command == \"delete\"` handler.\n- Accepts payload: `{command: \"delete\", paths: [\"/runpod-volume/ComfyUI/models/loras/old.safetensors\", ...]}`.\n- Returns per-path result: `[{path, deleted: bool, error?: string}]`.\n- **Security: validate all paths are under `/runpod-volume`** to prevent path traversal (no `..`, no symlink escapes). Reject any path that doesn't start with the volume root.\n- Update `worker.py` (~lines 617-626) to dispatch the new command.\n\n**Acceptance:**\n- [ ] `command: \"delete\"` job accepts a list of paths and returns per-path delete result\n- [ ] Paths outside `/runpod-volume` are rejected with a clear error (never deleted)\n- [ ] Missing files return `{deleted: false, error: \"not found\"}` (idempotent, no exception)\n- [ ] Documented in ComfyGen docs with the security constraint called out\n\n**Effort estimate (from audit):** 1-2 hours, ~80 LOC\n\n**Cross-project reference:** BlockFlow `sgs-ui-wisp-las.3` (Preset installer). Hard dependency for the uninstall flow. Also enables BlockFlow's preset update flow (orphaned-file cleanup before downloading new version) and Storage tab in Settings.","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-21T07:48:36Z","created_by":"Hearmeman24","updated_at":"2026-05-21T08:37:42Z","started_at":"2026-05-21T08:34:57Z","closed_at":"2026-05-21T08:37:42Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-h3o","title":"Add sha256 verification + content-addressable dedup to download handler (BlockFlow OSS push)","description":"**Context:** BlockFlow's preset installer (`sgs-ui-wisp-las.3`) needs to verify downloaded model files match expected SHA256 hashes (from the preset's `models.json` manifest). Without this, corrupted/partial downloads silently produce broken presets. The installer also wants to skip re-downloading identical files to save bandwidth on update / retry / resume flows.\n\n**Current state (audit):** `serverless-runtime/download_handler.py` already accepts a list of download entries and reports per-file progress (good). But:\n- **No SHA256 verification** — files are downloaded via `aria2c` (lines 103-190) but never hashed against expected values. The schema accepts no `sha256` field.\n- **No dedup** — lines 44-46 do a pre-download \"does file exist?\" check (binary), but no content-based dedup. Re-downloads happen even when content is identical.\n- **Schema mismatch with BlockFlow manifest:** ComfyGen accepts `{source: \"civitai\"|\"url\", url, dest, filename, version_id}` per entry. BlockFlow's preset manifest is `{url, sha256, destination_path}`. BlockFlow's installer will translate, but having ComfyGen accept the preset shape directly is cleaner.\n\n**What's needed:**\n- Add optional `sha256` field to each download entry. If present, compute hash post-download (line ~180-190 in `download_handler.py` per audit) and fail the job on mismatch.\n- Add a content-addressable cache: before re-downloading a URL whose SHA256 is already known + matches a file on disk, skip and return \"cached\".\n- Optionally: accept `destination_path` as a synonym for current `dest + filename` to reduce translation burden on BlockFlow.\n\n**Acceptance:**\n- [ ] Download job accepts `sha256` per entry (optional, backwards compatible)\n- [ ] On completion, returns per-file `{path, sha256, bytes, cached: bool}` so the caller can verify\n- [ ] If `sha256` provided and post-download hash mismatches, job fails with a clear error (does not leave a corrupt file on disk)\n- [ ] If `sha256` provided and a file already exists at the destination with the same hash, return `cached: true` without re-downloading\n- [ ] Documented in `serverless-runtime/download_handler.py` docstring + ComfyGen docs\n\n**Effort estimate (from audit):** 3-4 hours, ~150 LOC\n\n**Cross-project reference:** BlockFlow `sgs-ui-wisp-las.3` (Preset installer). Acceptance criterion: \"Sha256 verification: each downloaded file's hash compared to manifest expected. Mismatch → preset stays not-installed, error surfaced.\"","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-21T07:48:35Z","created_by":"Hearmeman24","updated_at":"2026-05-21T08:37:48Z","started_at":"2026-05-21T08:34:46Z","closed_at":"2026-05-21T08:37:48Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-c62","title":"Add /health worker command returning version (BlockFlow OSS push)","description":"**Context:** BlockFlow's OSS push needs a worker-level `/health` endpoint that returns the ComfyGen version. This gates preset compatibility (each preset in the upcoming `Hearmeman24/blockflow-presets` registry declares a `comfygen_min_version`; BlockFlow refuses install if the worker is older) and powers the \"attach existing endpoint\" flow in BlockFlow's Settings page (paste endpoint ID → BlockFlow calls /health to verify reachability + record version).\n\n**Current state (audit):** No worker-level health command exists. `serverless-runtime/worker.py` dispatches only `download`, `list_models`, `query_info`. RunPod's built-in `/v2/{id}/health` only reports worker counts, not the worker's own version. ComfyGen version `0.2.0` lives in `pyproject.toml` but is never exposed at runtime.\n\n**What's needed:**\n- New file `serverless-runtime/health_handler.py` exposing a handler that returns `{ok: true, version: \"\u003cfrom pyproject.toml or hardcoded constant\u003e\"}`.\n- Update `worker.py` (~lines 617-626 per the audit) to dispatch `command == \"health\"`.\n\n**Acceptance:**\n- [ ] `command: \"health\"` job returns `{ok: true, version: \"X.Y.Z\"}` where version matches the installed ComfyGen package version\n- [ ] Returns quickly (no model loading, no GPU work) so the BlockFlow attach-flow validation is snappy\n- [ ] Documented in ComfyGen's README/CLAUDE.md as part of the worker job command set\n\n**Effort estimate (from audit):** 1-2 hours, ~50 LOC\n\n**Cross-project reference:** BlockFlow epic `sgs-ui-wisp-las`, specifically beads `.2` (Setup wizard's attach-existing flow), `.3` (Preset installer's version-gating), `.10` (Preset registry's `comfygen_min_version` field).","notes":"BOOTSTRAPS TEST INFRA — this is the first issue implemented in this repo, so it ALSO carries the responsibility of setting up pytest infrastructure:\n\n1. Add pytest + pytest-mock as dev dependencies (uv add --dev or equivalent)\n2. Create tests/ directory + conftest.py with fixtures for serverless-runtime handler tests\n3. Write the /health handler test FIRST: 'test_health_returns_ok_and_version()' — should fail because no health command exists yet\n4. Watch it fail with the expected error (unknown command)\n5. Implement minimal /health handler to make it pass\n6. Watch it pass\n7. Document the test setup in README + add to ComfyGen CLAUDE.md (if absent)\n\nSubsequent ComfyGen beads (-h3o sha256, -ema volume_info, -f9d delete) all follow the same test-first cycle but inherit the bootstrapped infra.","status":"closed","priority":2,"issue_type":"feature","assignee":"Hearmeman24","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-21T07:48:34Z","created_by":"Hearmeman24","updated_at":"2026-05-21T08:33:34Z","started_at":"2026-05-21T08:31:41Z","closed_at":"2026-05-21T08:33:34Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"remote_comfy_generator-4bs","title":"[upstream PR] Submit cache-invalidation fix to kijai/ComfyUI-WanAnimatePreprocess #32","description":"**Scope:** open a PR to kijai/ComfyUI-WanAnimatePreprocess that applies the fix from issue #32 (cache invalidation + extension registration). The exact diff is already in the issue body. Once merged, the local hotpatch (separate bead) can be removed.\n\n**Acceptance:** PR opened, link recorded in this bead.","status":"open","priority":3,"issue_type":"task","owner":"Hearmeman24@users.noreply.github.com","created_at":"2026-05-23T16:47:16Z","created_by":"Hearmeman24","updated_at":"2026-05-23T16:47:16Z","dependency_count":0,"dependent_count":1,"comment_count":0}
